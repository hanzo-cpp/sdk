/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include "hanzo/model/Vector_PointId.h"

namespace hanzo {
namespace model {

void Vector_PointId::validate()
{
    // A oneOf is validated by the alternative it holds.
}

const Vector_PointId::VariantType& Vector_PointId::getVariant() const
{
    return m_variantValue;
}

void Vector_PointId::setVariant(Vector_PointId::VariantType value)
{
    m_variantValue = value;
}

web::json::value Vector_PointId::toJson() const
{
    web::json::value val = web::json::value::object();

    std::visit([&](auto&& arg) {
        using T = std::decay_t<decltype(arg)>;
        if constexpr (std::is_same_v<T, std::monostate>) {
            val = web::json::value::null();
        } else {
            val = ModelBase::toJson(arg);
        }
    }, m_variantValue);

    return val;
}

void Vector_PointId::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    std::visit([&](auto&& arg) {
        using T = std::decay_t<decltype(arg)>;
        if constexpr (!std::is_same_v<T, std::monostate>) {
          multipart->add(ModelBase::toHttpContent(prefix, arg));
        }
    }, m_variantValue);
}

bool Vector_PointId::fromJson(const web::json::value& json)
{
    // oneOf: the first alternative that accepts the document wins, in the order
    // the schema declares them.
    {
        int32_t target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        utility::string_t target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    return false;
}

bool Vector_PointId::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix)
{
    {
        int32_t target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        utility::string_t target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    return false;
}

template bool Vector_PointId::fromJson<int32_t>(const web::json::value& json);
template bool Vector_PointId::fromMultiPart<int32_t>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);
template bool Vector_PointId::fromJson<utility::string_t>(const web::json::value& json);
template bool Vector_PointId::fromMultiPart<utility::string_t>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);

}
}


