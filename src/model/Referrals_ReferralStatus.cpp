/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include "hanzo/model/Referrals_ReferralStatus.h"

namespace hanzo {
namespace model {

namespace
{
using EnumUnderlyingType = utility::string_t;

Referrals_ReferralStatus::eReferrals_ReferralStatus toEnum(const EnumUnderlyingType& val)
{
    if (val == utility::conversions::to_string_t(_XPLATSTR("signed_up")))
        return Referrals_ReferralStatus::eReferrals_ReferralStatus::SIGNED_UP;
    if (val == utility::conversions::to_string_t(_XPLATSTR("qualified")))
        return Referrals_ReferralStatus::eReferrals_ReferralStatus::QUALIFIED;
    if (val == utility::conversions::to_string_t(_XPLATSTR("credited")))
        return Referrals_ReferralStatus::eReferrals_ReferralStatus::CREDITED;
    return {};
}

EnumUnderlyingType fromEnum(Referrals_ReferralStatus::eReferrals_ReferralStatus e)
{
    switch (e)
    {
    case Referrals_ReferralStatus::eReferrals_ReferralStatus::SIGNED_UP:
        return _XPLATSTR("signed_up");
    case Referrals_ReferralStatus::eReferrals_ReferralStatus::QUALIFIED:
        return _XPLATSTR("qualified");
    case Referrals_ReferralStatus::eReferrals_ReferralStatus::CREDITED:
        return _XPLATSTR("credited");
    default:
        break;
    }
    return {};
}
}

Referrals_ReferralStatus::Referrals_ReferralStatus()
{
}

Referrals_ReferralStatus::~Referrals_ReferralStatus()
{
}

void Referrals_ReferralStatus::validate()
{
    // TODO: implement validation
}

web::json::value Referrals_ReferralStatus::toJson() const
{
    auto val = fromEnum(m_value);
    return web::json::value(val);
}

bool Referrals_ReferralStatus::fromJson(const web::json::value& val)
{
    m_value = toEnum(val.as_string());
    return true;
}

void Referrals_ReferralStatus::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }

    auto e = fromEnum(m_value);
    multipart->add(ModelBase::toHttpContent(namePrefix, e));
}

bool Referrals_ReferralStatus::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    bool ok = true;
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }
    {
        EnumUnderlyingType e;
        ok = ModelBase::fromHttpContent(multipart->getContent(namePrefix), e);
        if (ok)
        {
            auto v = toEnum(e);
            setValue(v);
        }
    }
    return ok;
}

Referrals_ReferralStatus::eReferrals_ReferralStatus Referrals_ReferralStatus::getValue() const
{
   return m_value;
}

void Referrals_ReferralStatus::setValue(Referrals_ReferralStatus::eReferrals_ReferralStatus const value)
{
   m_value = value;
}

Referrals_ReferralStatus::Referrals_ReferralStatus(utility::string_t str){
    setValue( strToEnumMap[str] );
}

}
}


