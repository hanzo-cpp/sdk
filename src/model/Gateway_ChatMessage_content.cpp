/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include "hanzo/model/Gateway_ChatMessage_content.h"

namespace hanzo {
namespace model {

void Gateway_ChatMessage_content::validate()
{
    // A oneOf is validated by the alternative it holds.
}

const Gateway_ChatMessage_content::VariantType& Gateway_ChatMessage_content::getVariant() const
{
    return m_variantValue;
}

void Gateway_ChatMessage_content::setVariant(Gateway_ChatMessage_content::VariantType value)
{
    m_variantValue = value;
}

web::json::value Gateway_ChatMessage_content::toJson() const
{
    web::json::value val = web::json::value::object();

    std::visit([&](auto&& arg) {
        using T = std::decay_t<decltype(arg)>;
        if constexpr (std::is_same_v<T, std::monostate>) {
            val = web::json::value::null();
        } else {
            val = ModelBase::toJson(arg);
        }
    }, m_variantValue);

    return val;
}

void Gateway_ChatMessage_content::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    std::visit([&](auto&& arg) {
        using T = std::decay_t<decltype(arg)>;
        if constexpr (!std::is_same_v<T, std::monostate>) {
          multipart->add(ModelBase::toHttpContent(prefix, arg));
        }
    }, m_variantValue);
}

bool Gateway_ChatMessage_content::fromJson(const web::json::value& json)
{
    // oneOf: the first alternative that accepts the document wins, in the order
    // the schema declares them.
    {
        std::vector<std::shared_ptr<Object>> target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        utility::string_t target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    return false;
}

bool Gateway_ChatMessage_content::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix)
{
    {
        std::vector<std::shared_ptr<Object>> target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        utility::string_t target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    return false;
}

template bool Gateway_ChatMessage_content::fromJson<std::vector<std::shared_ptr<Object>>>(const web::json::value& json);
template bool Gateway_ChatMessage_content::fromMultiPart<std::vector<std::shared_ptr<Object>>>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);
template bool Gateway_ChatMessage_content::fromJson<utility::string_t>(const web::json::value& json);
template bool Gateway_ChatMessage_content::fromMultiPart<utility::string_t>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);

}
}


