/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include "hanzo/model/Gateway_createCompletion_request_prompt.h"

namespace hanzo {
namespace model {

void Gateway_createCompletion_request_prompt::validate()
{
    // A oneOf is validated by the alternative it holds.
}

const Gateway_createCompletion_request_prompt::VariantType& Gateway_createCompletion_request_prompt::getVariant() const
{
    return m_variantValue;
}

void Gateway_createCompletion_request_prompt::setVariant(Gateway_createCompletion_request_prompt::VariantType value)
{
    m_variantValue = value;
}

web::json::value Gateway_createCompletion_request_prompt::toJson() const
{
    web::json::value val = web::json::value::object();

    std::visit([&](auto&& arg) {
        using T = std::decay_t<decltype(arg)>;
        if constexpr (std::is_same_v<T, std::monostate>) {
            val = web::json::value::null();
        } else {
            val = ModelBase::toJson(arg);
        }
    }, m_variantValue);

    return val;
}

void Gateway_createCompletion_request_prompt::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    std::visit([&](auto&& arg) {
        using T = std::decay_t<decltype(arg)>;
        if constexpr (!std::is_same_v<T, std::monostate>) {
          multipart->add(ModelBase::toHttpContent(prefix, arg));
        }
    }, m_variantValue);
}

bool Gateway_createCompletion_request_prompt::fromJson(const web::json::value& json)
{
    // oneOf: the first alternative that accepts the document wins, in the order
    // the schema declares them.
    {
        std::vector<utility::string_t> target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        utility::string_t target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    return false;
}

bool Gateway_createCompletion_request_prompt::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix)
{
    {
        std::vector<utility::string_t> target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        utility::string_t target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    return false;
}

template bool Gateway_createCompletion_request_prompt::fromJson<std::vector<utility::string_t>>(const web::json::value& json);
template bool Gateway_createCompletion_request_prompt::fromMultiPart<std::vector<utility::string_t>>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);
template bool Gateway_createCompletion_request_prompt::fromJson<utility::string_t>(const web::json::value& json);
template bool Gateway_createCompletion_request_prompt::fromMultiPart<utility::string_t>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);

}
}


