/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include "hanzo/model/Vector_Condition.h"

namespace hanzo {
namespace model {

void Vector_Condition::validate()
{
    // A oneOf is validated by the alternative it holds.
}

const Vector_Condition::VariantType& Vector_Condition::getVariant() const
{
    return m_variantValue;
}

void Vector_Condition::setVariant(Vector_Condition::VariantType value)
{
    m_variantValue = value;
}

web::json::value Vector_Condition::toJson() const
{
    web::json::value val = web::json::value::object();

    std::visit([&](auto&& arg) {
        using T = std::decay_t<decltype(arg)>;
        if constexpr (std::is_same_v<T, std::monostate>) {
            val = web::json::value::null();
        } else {
            val = ModelBase::toJson(arg);
        }
    }, m_variantValue);

    return val;
}

void Vector_Condition::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    std::visit([&](auto&& arg) {
        using T = std::decay_t<decltype(arg)>;
        if constexpr (!std::is_same_v<T, std::monostate>) {
          multipart->add(ModelBase::toHttpContent(prefix, arg));
        }
    }, m_variantValue);
}

bool Vector_Condition::fromJson(const web::json::value& json)
{
    // oneOf: the first alternative that accepts the document wins, in the order
    // the schema declares them.
    {
        Vector_MatchCondition target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        Vector_RangeCondition target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    return false;
}

bool Vector_Condition::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix)
{
    {
        Vector_MatchCondition target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        Vector_RangeCondition target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    return false;
}

template bool Vector_Condition::fromJson<Vector_MatchCondition>(const web::json::value& json);
template bool Vector_Condition::fromMultiPart<Vector_MatchCondition>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);
template bool Vector_Condition::fromJson<Vector_RangeCondition>(const web::json::value& json);
template bool Vector_Condition::fromMultiPart<Vector_RangeCondition>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);

}
}


