/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


#include "hanzo/model/Tracker_IssuePriority.h"

namespace hanzo {
namespace model {

namespace
{
using EnumUnderlyingType = utility::string_t;

Tracker_IssuePriority::eTracker_IssuePriority toEnum(const EnumUnderlyingType& val)
{
    if (val == utility::conversions::to_string_t(_XPLATSTR("none")))
        return Tracker_IssuePriority::eTracker_IssuePriority::NONE;
    if (val == utility::conversions::to_string_t(_XPLATSTR("urgent")))
        return Tracker_IssuePriority::eTracker_IssuePriority::URGENT;
    if (val == utility::conversions::to_string_t(_XPLATSTR("high")))
        return Tracker_IssuePriority::eTracker_IssuePriority::HIGH;
    if (val == utility::conversions::to_string_t(_XPLATSTR("medium")))
        return Tracker_IssuePriority::eTracker_IssuePriority::MEDIUM;
    if (val == utility::conversions::to_string_t(_XPLATSTR("low")))
        return Tracker_IssuePriority::eTracker_IssuePriority::LOW;
    return {};
}

EnumUnderlyingType fromEnum(Tracker_IssuePriority::eTracker_IssuePriority e)
{
    switch (e)
    {
    case Tracker_IssuePriority::eTracker_IssuePriority::NONE:
        return _XPLATSTR("none");
    case Tracker_IssuePriority::eTracker_IssuePriority::URGENT:
        return _XPLATSTR("urgent");
    case Tracker_IssuePriority::eTracker_IssuePriority::HIGH:
        return _XPLATSTR("high");
    case Tracker_IssuePriority::eTracker_IssuePriority::MEDIUM:
        return _XPLATSTR("medium");
    case Tracker_IssuePriority::eTracker_IssuePriority::LOW:
        return _XPLATSTR("low");
    default:
        break;
    }
    return {};
}
}

Tracker_IssuePriority::Tracker_IssuePriority()
{
}

Tracker_IssuePriority::~Tracker_IssuePriority()
{
}

void Tracker_IssuePriority::validate()
{
    // TODO: implement validation
}

web::json::value Tracker_IssuePriority::toJson() const
{
    auto val = fromEnum(m_value);
    return web::json::value(val);
}

bool Tracker_IssuePriority::fromJson(const web::json::value& val)
{
    m_value = toEnum(val.as_string());
    return true;
}

void Tracker_IssuePriority::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }

    auto e = fromEnum(m_value);
    multipart->add(ModelBase::toHttpContent(namePrefix, e));
}

bool Tracker_IssuePriority::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    bool ok = true;
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }
    {
        EnumUnderlyingType e;
        ok = ModelBase::fromHttpContent(multipart->getContent(namePrefix), e);
        if (ok)
        {
            auto v = toEnum(e);
            setValue(v);
        }
    }
    return ok;
}

Tracker_IssuePriority::eTracker_IssuePriority Tracker_IssuePriority::getValue() const
{
   return m_value;
}

void Tracker_IssuePriority::setValue(Tracker_IssuePriority::eTracker_IssuePriority const value)
{
   m_value = value;
}

Tracker_IssuePriority::Tracker_IssuePriority(utility::string_t str){
    setValue( strToEnumMap[str] );
}

}
}


