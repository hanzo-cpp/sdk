/**
 * Hanzo Cloud API
 * Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.14.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "hanzo/model/Post_event_request.h"

namespace hanzo {
namespace model {

void Post_event_request::validate()
{
    // A oneOf is validated by the alternative it holds.
}

const Post_event_request::VariantType& Post_event_request::getVariant() const
{
    return m_variantValue;
}

void Post_event_request::setVariant(Post_event_request::VariantType value)
{
    m_variantValue = value;
}

web::json::value Post_event_request::toJson() const
{
    web::json::value val = web::json::value::object();

    std::visit([&](auto&& arg) {
        using T = std::decay_t<decltype(arg)>;
        if constexpr (std::is_same_v<T, std::monostate>) {
            val = web::json::value::null();
        } else {
            val = ModelBase::toJson(arg);
        }
    }, m_variantValue);

    return val;
}

void Post_event_request::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    std::visit([&](auto&& arg) {
        using T = std::decay_t<decltype(arg)>;
        if constexpr (!std::is_same_v<T, std::monostate>) {
          multipart->add(ModelBase::toHttpContent(prefix, arg));
        }
    }, m_variantValue);
}

bool Post_event_request::fromJson(const web::json::value& json)
{
    // oneOf: the first alternative that accepts the document wins, in the order
    // the schema declares them.
    {
        CaptureBatch target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        Event target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        InsightsBody target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        std::vector<std::shared_ptr<Event>> target;
        if (ModelBase::fromJson(json, target))
        {
            m_variantValue = target;
            return true;
        }
    }
    return false;
}

bool Post_event_request::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix)
{
    {
        CaptureBatch target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        Event target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        InsightsBody target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    {
        std::vector<std::shared_ptr<Event>> target;
        if (ModelBase::fromHttpContent(multipart->getContent(namePrefix), target))
        {
            m_variantValue = target;
            return true;
        }
    }
    return false;
}

template bool Post_event_request::fromJson<CaptureBatch>(const web::json::value& json);
template bool Post_event_request::fromMultiPart<CaptureBatch>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);
template bool Post_event_request::fromJson<Event>(const web::json::value& json);
template bool Post_event_request::fromMultiPart<Event>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);
template bool Post_event_request::fromJson<InsightsBody>(const web::json::value& json);
template bool Post_event_request::fromMultiPart<InsightsBody>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);
template bool Post_event_request::fromJson<std::vector<std::shared_ptr<Event>>>(const web::json::value& json);
template bool Post_event_request::fromMultiPart<std::vector<std::shared_ptr<Event>>>(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix);

}
}


