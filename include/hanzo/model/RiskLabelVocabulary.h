/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.14.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * RiskLabelVocabulary.h
 *
 * 
 */

#ifndef HANZO_MODEL_RiskLabelVocabulary_H_
#define HANZO_MODEL_RiskLabelVocabulary_H_

#include <boost/optional.hpp>

#include "hanzo/ModelBase.h"

#include <cpprest/details/basic_types.h>
#include <vector>

namespace hanzo {
namespace model {



class  RiskLabelVocabulary
    : public ModelBase
{
public:
    RiskLabelVocabulary();
    virtual ~RiskLabelVocabulary();

    /////////////////////////////////////////////
    /// ModelBase overrides

    void validate() override;

    web::json::value toJson() const override;
    bool fromJson(const web::json::value& json) override;

    void toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) const override;
    bool fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) override;


    /////////////////////////////////////////////
    /// RiskLabelVocabulary members


    /// <summary>
    /// Dispositions is the closed set a write&#39;s &#x60;disposition&#x60; must be drawn from, published in full so a caller can validate a batch before filing it instead of discovering a refusal per member: \&quot;productive\&quot;, \&quot;unproductive\&quot;, and \&quot;\&quot; — the EMPTY STRING is a member and means an explicit unjudged, so a client that filters empties out of this list drops a third of the vocabulary and can never file \&quot;we looked and could not say\&quot;. They are the AML engine&#39;s own spelling, verbatim, which is what lets a replay there report against these values.
    /// </summary>
    std::vector<utility::string_t> getDispositions() const;
    bool dispositionsIsSet() const;
    void unsetDispositions();
    void setDispositions(const std::vector<utility::string_t>& value);

    /// <summary>
    /// Kinds, Dispositions and Sources are the closed vocabularies. A value outside them is refused at the door.
    /// </summary>
    std::vector<utility::string_t> getKinds() const;
    bool kindsIsSet() const;
    void unsetKinds();
    void setKinds(const std::vector<utility::string_t>& value);

    /// <summary>
    /// Precedence is the sources in the order that resolves a conflict, strongest first. It is DERIVED from the same declaration the resolver reads, so the published order is the enforced order and cannot drift from it.
    /// </summary>
    std::vector<utility::string_t> getPrecedence() const;
    bool precedenceIsSet() const;
    void unsetPrecedence();
    void setPrecedence(const std::vector<utility::string_t>& value);

    /// <summary>
    /// Retention is the platform floor in days: no tenant may dispose of a label younger than this, because a label can be the input to an adverse action.
    /// </summary>
    int32_t getRetention() const;
    bool retentionIsSet() const;
    void unsetRetention();
    void setRetention(int32_t value);

    /// <summary>
    /// Rule states the tie-breaks below rank, in order, so a caller reading a contested resolution can reproduce it.
    /// </summary>
    std::vector<utility::string_t> getRule() const;
    bool ruleIsSet() const;
    void unsetRule();
    void setRule(const std::vector<utility::string_t>& value);


protected:
    std::vector<utility::string_t> m_Dispositions;
    bool m_DispositionsIsSet;

    std::vector<utility::string_t> m_Kinds;
    bool m_KindsIsSet;

    std::vector<utility::string_t> m_Precedence;
    bool m_PrecedenceIsSet;

    int32_t m_Retention;
    bool m_RetentionIsSet;

    std::vector<utility::string_t> m_Rule;
    bool m_RuleIsSet;

};


}
}

#endif /* HANZO_MODEL_RiskLabelVocabulary_H_ */
