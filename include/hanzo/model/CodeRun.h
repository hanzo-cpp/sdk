/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.14.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * CodeRun.h
 *
 * 
 */

#ifndef HANZO_MODEL_CodeRun_H_
#define HANZO_MODEL_CodeRun_H_

#include <boost/optional.hpp>

#include "hanzo/ModelBase.h"

#include <cpprest/details/basic_types.h>
#include "hanzo/model/CodeFile.h"
#include <vector>

namespace hanzo {
namespace model {

class CodeFile;


class  CodeRun
    : public ModelBase
{
public:
    CodeRun();
    virtual ~CodeRun();

    /////////////////////////////////////////////
    /// ModelBase overrides

    void validate() override;

    web::json::value toJson() const override;
    bool fromJson(const web::json::value& json) override;

    void toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) const override;
    bool fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) override;


    /////////////////////////////////////////////
    /// CodeRun members


    /// <summary>
    /// Args become the PROGRAM&#39;s argv, never the compiler&#39;s. For the compiled languages the toolchain builds first and these are passed to the binary it produced.
    /// </summary>
    std::vector<utility::string_t> getArgs() const;
    bool argsIsSet() const;
    void unsetArgs();
    void setArgs(const std::vector<utility::string_t>& value);

    /// <summary>
    /// Code is the WHOLE program, not a fragment: it is written to a single file and that file is what runs, so a compiled language needs its entry point and an interpreted one runs top to bottom.
    /// </summary>
    utility::string_t getCode() const;
    bool codeIsSet() const;
    void unsetCode();
    void setCode(const utility::string_t& value);

    /// <summary>
    /// Files are inputs the host already put in some session. Each names the session its bytes live in, which is usually — and ideally — the session this run wants.
    /// </summary>
    std::vector<std::shared_ptr<CodeFile>> getFiles() const;
    bool filesIsSet() const;
    void unsetFiles();
    void setFiles(const std::vector<std::shared_ptr<CodeFile>>& value);

    /// <summary>
    /// Lang selects the toolchain, and with it the filename the code is written to and the line that runs it: py, js, ts, bash, r, php, go, rs, c, cpp, java, d, f90. Anything else is refused rather than guessed at — a run in the wrong language fails somewhere deep in a compiler, which reads as an outage.
    /// </summary>
    utility::string_t getLang() const;
    bool langIsSet() const;
    void unsetLang();
    void setLang(const utility::string_t& value);

    /// <summary>
    /// RuntimeSessionHint is the stateful-session hint. It is carried so a client that sends it is not silently misread, and it selects nothing here: every session in this implementation is already a warm sandbox, so there is no second kind of runtime for a hint to choose between.
    /// </summary>
    utility::string_t getRuntimeSessionHint() const;
    bool runtimeSessionHintIsSet() const;
    void unsetRuntime_session_hint();
    void setRuntimeSessionHint(const utility::string_t& value);

    /// <summary>
    /// SessionID continues an EXISTING sandbox, which is what makes runs stateful: the same filesystem, so one run&#39;s output file is the next run&#39;s input. Empty leases a fresh sandbox and the id it got comes back on the result.
    /// </summary>
    utility::string_t getSessionId() const;
    bool sessionIdIsSet() const;
    void unsetSession_id();
    void setSessionId(const utility::string_t& value);

    /// <summary>
    /// UserID attributes the run inside the caller&#39;s org. It is a label, never a tenant: the org is resolved from the validated principal and a value here cannot widen what the run may reach.
    /// </summary>
    utility::string_t getUserId() const;
    bool userIdIsSet() const;
    void unsetUser_id();
    void setUserId(const utility::string_t& value);


protected:
    std::vector<utility::string_t> m_Args;
    bool m_ArgsIsSet;

    utility::string_t m_Code;
    bool m_CodeIsSet;

    std::vector<std::shared_ptr<CodeFile>> m_Files;
    bool m_FilesIsSet;

    utility::string_t m_Lang;
    bool m_LangIsSet;

    utility::string_t m_Runtime_session_hint;
    bool m_Runtime_session_hintIsSet;

    utility::string_t m_Session_id;
    bool m_Session_idIsSet;

    utility::string_t m_User_id;
    bool m_User_idIsSet;

};


}
}

#endif /* HANZO_MODEL_CodeRun_H_ */
