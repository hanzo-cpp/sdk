/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.14.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * EdgeState.h
 *
 * 
 */

#ifndef HANZO_MODEL_EdgeState_H_
#define HANZO_MODEL_EdgeState_H_

#include <boost/optional.hpp>

#include "hanzo/ModelBase.h"

#include <cpprest/details/basic_types.h>
#include <map>
#include <vector>

namespace hanzo {
namespace model {



class  EdgeState
    : public ModelBase
{
public:
    EdgeState();
    virtual ~EdgeState();

    /////////////////////////////////////////////
    /// ModelBase overrides

    void validate() override;

    web::json::value toJson() const override;
    bool fromJson(const web::json::value& json) override;

    void toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) const override;
    bool fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) override;


    /////////////////////////////////////////////
    /// EdgeState members


    /// <summary>
    /// Configured is whether the edge holds credentials to act at all. False means every purge is a no-op.
    /// </summary>
    bool isConfigured() const;
    bool configuredIsSet() const;
    void unsetConfigured();
    void setConfigured(bool value);

    /// <summary>
    /// Error is the blocker, so an operator reads it instead of guessing at it.
    /// </summary>
    utility::string_t getError() const;
    bool errorIsSet() const;
    void unsetError();
    void setError(const utility::string_t& value);

    /// <summary>
    /// Freshness says, in one phrase, how long after a publish a reader sees it. It is the sentence an operator actually wants; the booleans above are how a machine reads the same fact.
    /// </summary>
    utility::string_t getFreshness() const;
    bool freshnessIsSet() const;
    void unsetFreshness();
    void setFreshness(const utility::string_t& value);

    /// <summary>
    /// Policy is the Cache-Control this edge serves each class of object with. It is DERIVED from the one canonical function, never a second copy: half the confusion when a publish looks stale is not knowing what the TTLs are, and reading them out of the source is not something an operator should have to do to answer \&quot;how long until this is live\&quot;.
    /// </summary>
    std::map<utility::string_t, utility::string_t> getPolicy() const;
    bool policyIsSet() const;
    void unsetPolicy();
    void setPolicy(const std::map<utility::string_t, utility::string_t>& value);

    /// <summary>
    /// Provider is the CDN behind this edge, or \&quot;none\&quot;. It is the first thing an operator wants and the only vendor name this API returns.
    /// </summary>
    utility::string_t getProvider() const;
    bool providerIsSet() const;
    void unsetProvider();
    void setProvider(const utility::string_t& value);

    /// <summary>
    /// Reach is the apexes a publish is invalidated on. A site is served on more than one — the site plane&#39;s own and the first-party apex — and a purge that covers one of them looks identical from here to a purge that covers both.
    /// </summary>
    std::vector<utility::string_t> getReach() const;
    bool reachIsSet() const;
    void unsetReach();
    void setReach(const std::vector<utility::string_t>& value);

    /// <summary>
    /// Status is \&quot;ok\&quot; when a publish reaches readers immediately, else \&quot;degraded\&quot;.
    /// </summary>
    utility::string_t getStatus() const;
    bool statusIsSet() const;
    void unsetStatus();
    void setStatus(const utility::string_t& value);


protected:
    bool m_Configured;
    bool m_ConfiguredIsSet;

    utility::string_t m_Error;
    bool m_ErrorIsSet;

    utility::string_t m_Freshness;
    bool m_FreshnessIsSet;

    std::map<utility::string_t, utility::string_t> m_Policy;
    bool m_PolicyIsSet;

    utility::string_t m_Provider;
    bool m_ProviderIsSet;

    std::vector<utility::string_t> m_Reach;
    bool m_ReachIsSet;

    utility::string_t m_Status;
    bool m_StatusIsSet;

};


}
}

#endif /* HANZO_MODEL_EdgeState_H_ */
