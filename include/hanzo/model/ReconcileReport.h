/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.14.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * ReconcileReport.h
 *
 * 
 */

#ifndef HANZO_MODEL_ReconcileReport_H_
#define HANZO_MODEL_ReconcileReport_H_

#include <boost/optional.hpp>

#include "hanzo/ModelBase.h"

#include "hanzo/model/AppliedResource.h"
#include "hanzo/model/ReconcileSource.h"
#include <cpprest/details/basic_types.h>
#include <vector>

namespace hanzo {
namespace model {

class AppliedResource;
class ReconcileSource;


class  ReconcileReport
    : public ModelBase
{
public:
    ReconcileReport();
    virtual ~ReconcileReport();

    /////////////////////////////////////////////
    /// ModelBase overrides

    void validate() override;

    web::json::value toJson() const override;
    bool fromJson(const web::json::value& json) override;

    void toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) const override;
    bool fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) override;


    /////////////////////////////////////////////
    /// ReconcileReport members


    /// <summary>
    /// Declared is how many objects the rendered source declares — the denominator the three outcome counts below are read against. Zero means the render produced nothing, which trips the prune fuse rather than sweeping the fleet.
    /// </summary>
    int32_t getDeclared() const;
    bool declaredIsSet() const;
    void unsetDeclared();
    void setDeclared(int32_t value);

    /// <summary>
    /// Failed is how many objects the apply could not reconcile. Non-zero is a PARTIAL run reported at 200: the engine applied what it could and each failure names itself in Results, so a caller reads this number rather than the status code to learn whether the fleet matches the source.
    /// </summary>
    int32_t getFailed() const;
    bool failedIsSet() const;
    void unsetFailed();
    void setFailed(int32_t value);

    /// <summary>
    /// Instance is the tracking id this run stamps on everything it manages, so a later run can tell the objects it owns from objects another instance declares. DEPLOY_ENGINE_INSTANCE names it; the default is &#x60;universe&#x60;.
    /// </summary>
    utility::string_t getInstance() const;
    bool instanceIsSet() const;
    void unsetInstance();
    void setInstance(const utility::string_t& value);

    /// <summary>
    /// Prune reports whether DELETION was enabled for this run. False means an object the source no longer declares was left alone rather than removed, so a zero Pruned below means \&quot;nothing to delete\&quot; only when this is true.
    /// </summary>
    bool isPrune() const;
    bool pruneIsSet() const;
    void unsetPrune();
    void setPrune(bool value);

    /// <summary>
    /// Pruned is how many live objects this run DELETED because the source no longer declares them. Always 0 when Prune is false.
    /// </summary>
    int32_t getPruned() const;
    bool prunedIsSet() const;
    void unsetPruned();
    void setPruned(int32_t value);

    /// <summary>
    /// Results is one entry per object the run acted on, in the order the engine applied them. Empty (never null) when the run reconciled nothing.
    /// </summary>
    std::vector<std::shared_ptr<AppliedResource>> getResults() const;
    bool resultsIsSet() const;
    void unsetResults();
    void setResults(const std::vector<std::shared_ptr<AppliedResource>>& value);

    /// <summary>
    /// Revision is the source commit this run applied, as the source resolved it — a git commit SHA, not an image tag. It is what an operator cites when asking what the cluster was last made to match.
    /// </summary>
    utility::string_t getRevision() const;
    bool revisionIsSet() const;
    void unsetRevision();
    void setRevision(const utility::string_t& value);

    /// <summary>
    /// Source is the git coordinate the run rendered. It is this deployment&#39;s own configuration echoed back, never a request parameter, and it is reported so a reader of the answer knows WHICH tree the revision names.
    /// </summary>
    std::shared_ptr<ReconcileSource> getSource() const;
    bool sourceIsSet() const;
    void unsetSource();
    void setSource(const std::shared_ptr<ReconcileSource>& value);

    /// <summary>
    /// Synced is how many objects the run applied successfully.
    /// </summary>
    int32_t getSynced() const;
    bool syncedIsSet() const;
    void unsetSynced();
    void setSynced(int32_t value);


protected:
    int32_t m_Declared;
    bool m_DeclaredIsSet;

    int32_t m_Failed;
    bool m_FailedIsSet;

    utility::string_t m_Instance;
    bool m_InstanceIsSet;

    bool m_Prune;
    bool m_PruneIsSet;

    int32_t m_Pruned;
    bool m_PrunedIsSet;

    std::vector<std::shared_ptr<AppliedResource>> m_Results;
    bool m_ResultsIsSet;

    utility::string_t m_Revision;
    bool m_RevisionIsSet;

    std::shared_ptr<ReconcileSource> m_Source;
    bool m_SourceIsSet;

    int32_t m_Synced;
    bool m_SyncedIsSet;

};


}
}

#endif /* HANZO_MODEL_ReconcileReport_H_ */
