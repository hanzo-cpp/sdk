/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.14.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * SummaryResp.h
 *
 * 
 */

#ifndef HANZO_MODEL_SummaryResp_H_
#define HANZO_MODEL_SummaryResp_H_

#include <boost/optional.hpp>

#include "hanzo/ModelBase.h"

#include "hanzo/model/TotalView.h"
#include <cpprest/details/basic_types.h>
#include "hanzo/model/SourceState.h"
#include <vector>

namespace hanzo {
namespace model {

class SourceState;
class TotalView;


class  SummaryResp
    : public ModelBase
{
public:
    SummaryResp();
    virtual ~SummaryResp();

    /////////////////////////////////////////////
    /// ModelBase overrides

    void validate() override;

    web::json::value toJson() const override;
    bool fromJson(const web::json::value& json) override;

    void toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) const override;
    bool fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) override;


    /////////////////////////////////////////////
    /// SummaryResp members


    /// <summary>
    /// Account reports the linked-accounts ledger&#39;s own availability, so a partial answer never fabricates this half. It is scoped to the CALLER: the accounts they linked, metered from each provider&#39;s own login.
    /// </summary>
    std::shared_ptr<SourceState> getAccount() const;
    bool accountIsSet() const;
    void unsetAccount();
    void setAccount(const std::shared_ptr<SourceState>& value);

    /// <summary>
    /// From is when the window opens, RFC 3339 UTC. ONE resolver fixes it for both ledgers, so the account rows and the Hanzo rows always cover the same period — two resolvers could drift and turn the union into a lie.
    /// </summary>
    utility::string_t getFrom() const;
    bool fromIsSet() const;
    void unsetFrom();
    void setFrom(const utility::string_t& value);

    /// <summary>
    /// Hanzo reports the same for the Hanzo-routed ledger, which is scoped to the ORG rather than the caller — a different question over the same window. The two are independent: either can be unavailable while the other answers, and Rows then carries only the half that did.
    /// </summary>
    std::shared_ptr<SourceState> getHanzo() const;
    bool hanzoIsSet() const;
    void unsetHanzo();
    void setHanzo(const std::shared_ptr<SourceState>& value);

    /// <summary>
    /// Range is the resolved period label.
    /// </summary>
    utility::string_t getRange() const;
    bool rangeIsSet() const;
    void unsetRange();
    void setRange(const utility::string_t& value);

    /// <summary>
    /// Rows is the union of both ledgers, each row labelled by source and scope — concatenated, NEVER summed: a plan&#39;s percentage is not money.
    /// </summary>
    std::vector<std::shared_ptr<TotalView>> getRows() const;
    bool rowsIsSet() const;
    void unsetRows();
    void setRows(const std::vector<std::shared_ptr<TotalView>>& value);

    /// <summary>
    /// To is where the window closes, EXCLUSIVE, RFC 3339 UTC — the instant the read was served. Shared by both ledgers, for the reason From gives.
    /// </summary>
    utility::string_t getTo() const;
    bool toIsSet() const;
    void unsetTo();
    void setTo(const utility::string_t& value);


protected:
    std::shared_ptr<SourceState> m_Account;
    bool m_AccountIsSet;

    utility::string_t m_From;
    bool m_FromIsSet;

    std::shared_ptr<SourceState> m_Hanzo;
    bool m_HanzoIsSet;

    utility::string_t m_Range;
    bool m_RangeIsSet;

    std::vector<std::shared_ptr<TotalView>> m_Rows;
    bool m_RowsIsSet;

    utility::string_t m_To;
    bool m_ToIsSet;

};


}
}

#endif /* HANZO_MODEL_SummaryResp_H_ */
