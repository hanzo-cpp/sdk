/**
 * Hanzo Cloud API
 * Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.14.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * RiskCatalog.h
 *
 * 
 */

#ifndef HANZO_MODEL_RiskCatalog_H_
#define HANZO_MODEL_RiskCatalog_H_

#include <boost/optional.hpp>

#include "hanzo/ModelBase.h"

#include "hanzo/model/RiskModelFeature.h"
#include "hanzo/model/RiskBand.h"
#include "hanzo/model/RiskOrgFeature.h"
#include <cpprest/details/basic_types.h>
#include <vector>

namespace hanzo {
namespace model {

class RiskModelFeature;
class RiskBand;
class RiskOrgFeature;


class  RiskCatalog
    : public ModelBase
{
public:
    RiskCatalog();
    virtual ~RiskCatalog();

    /////////////////////////////////////////////
    /// ModelBase overrides

    void validate() override;

    web::json::value toJson() const override;
    bool fromJson(const web::json::value& json) override;

    void toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) const override;
    bool fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& namePrefix) override;


    /////////////////////////////////////////////
    /// RiskCatalog members


    /// <summary>
    /// Gap says why a lens could not be measured, when that is the case. Each reason names its own lens, because \&quot;the surface is unreadable\&quot; and \&quot;the network baseline is unreadable\&quot; are different facts.
    /// </summary>
    utility::string_t getGap() const;
    bool gapIsSet() const;
    void unsetGap();
    void setGap(const utility::string_t& value);

    /// <summary>
    /// Model is the governed inventory: one entry per dimension of the model space, each carrying the typology it serves and the published standard that asks for it. It is the same for every organisation, because it is the model&#39;s shape.
    /// </summary>
    std::vector<std::shared_ptr<RiskModelFeature>> getModel() const;
    bool modelIsSet() const;
    void unsetModel();
    void setModel(const std::vector<std::shared_ptr<RiskModelFeature>>& value);

    /// <summary>
    /// Network is the published cross-organisation baseline over the same window, so the surface above has something to be read AGAINST. It is the same for every caller and it names nobody.  It carries no tenant and cannot be made to: the table it reads has no org column, every figure is a quantile over at least kAnonOrgs organisations weighted one vote each, and a band that does not meet that floor is dropped on the way out.
    /// </summary>
    std::vector<std::shared_ptr<RiskBand>> getNetwork() const;
    bool networkIsSet() const;
    void unsetNetwork();
    void setNetwork(const std::vector<std::shared_ptr<RiskBand>>& value);

    /// <summary>
    /// Surface is what this organisation&#39;s own event surface carries, per dimension, measured over the window. A dimension present in no bucket is blind here — the model reads its neutral value and a reviewer has to be able to see that.
    /// </summary>
    std::vector<std::shared_ptr<RiskOrgFeature>> getSurface() const;
    bool surfaceIsSet() const;
    void unsetSurface();
    void setSurface(const std::vector<std::shared_ptr<RiskOrgFeature>>& value);

    /// <summary>
    /// Tenant is whose surface was measured.
    /// </summary>
    utility::string_t getTenant() const;
    bool tenantIsSet() const;
    void unsetTenant();
    void setTenant(const utility::string_t& value);


protected:
    utility::string_t m_Gap;
    bool m_GapIsSet;

    std::vector<std::shared_ptr<RiskModelFeature>> m_Model;
    bool m_ModelIsSet;

    std::vector<std::shared_ptr<RiskBand>> m_Network;
    bool m_NetworkIsSet;

    std::vector<std::shared_ptr<RiskOrgFeature>> m_Surface;
    bool m_SurfaceIsSet;

    utility::string_t m_Tenant;
    bool m_TenantIsSet;

};


}
}

#endif /* HANZO_MODEL_RiskCatalog_H_ */
