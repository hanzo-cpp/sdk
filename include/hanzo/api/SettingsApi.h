/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * SettingsApi.h
 *
 * 
 */

#ifndef HANZO_API_SettingsApi_H_
#define HANZO_API_SettingsApi_H_



#include "hanzo/ApiClient.h"

#include "hanzo/model/Cloud_settingsReq.h"
#include "hanzo/model/Cloud_settingsView.h"
#include "hanzo/model/Search_Settings.h"
#include "hanzo/model/Search_SummarizedTaskView.h"
#include <cpprest/details/basic_types.h>
#include <boost/optional.hpp>

namespace hanzo {
namespace api {

using namespace hanzo::model;



class  SettingsApi 
{
public:

    explicit SettingsApi( std::shared_ptr<const ApiClient> apiClient );

    virtual ~SettingsApi();

    /// <summary>
    /// GetSettings reads the caller org&#39;s configuration for one product, with every secret field MASKED — only the names of the set secrets come back, never their values, which live in KMS.
    /// </summary>
    /// <remarks>
    /// GetSettings reads the caller org&#39;s configuration for one product, with every secret field MASKED — only the names of the set secrets come back, never their values, which live in KMS. A product the org has never configured is not a 404: it answers 200 with an empty config object, so the console&#39;s Settings tab always renders and merges its own display defaults on top.
    /// </remarks>
    /// <param name="product">Product is the catalog slug, from the path. Must match ^[a-z0-9][a-z0-9._-]{0,62}$.</param>
    pplx::task<std::shared_ptr<Cloud_settingsView>> cloudGetV1SettingsProduct(
        utility::string_t product
    ) const;
    /// <summary>
    /// PutSettings writes the caller org&#39;s configuration for one product and answers the stored result, secrets masked.
    /// </summary>
    /// <remarks>
    /// PutSettings writes the caller org&#39;s configuration for one product and answers the stored result, secrets masked. Secret VALUES are sealed into KMS under orgs/{org}/settings/{product}/{key} and never touch this deployment&#39;s database; with no KMS configured a write that carries any secret is refused whole (503) rather than dropping it or persisting it in the clear. A secret the body omits keeps its stored value, so a partial write never silently clears one.
    /// </remarks>
    /// <param name="product">Product is the catalog slug, from the PATH. zip binds the path last, so the URL names the product being written whatever a body field claims.</param>
    /// <param name="cloudSettingsReq"></param>
    pplx::task<std::shared_ptr<Cloud_settingsView>> cloudPutV1SettingsProduct(
        utility::string_t product,
        std::shared_ptr<Cloud_settingsReq> cloudSettingsReq
    ) const;
    /// <summary>
    /// Get all index settings
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="indexUid">Unique index identifier</param>
    pplx::task<std::shared_ptr<Search_Settings>> searchGetSettings(
        utility::string_t indexUid
    ) const;
    /// <summary>
    /// Reset all settings to defaults
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="indexUid">Unique index identifier</param>
    pplx::task<std::shared_ptr<Search_SummarizedTaskView>> searchResetSettings(
        utility::string_t indexUid
    ) const;
    /// <summary>
    /// Update index settings
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="indexUid">Unique index identifier</param>
    /// <param name="searchSettings"></param>
    pplx::task<std::shared_ptr<Search_SummarizedTaskView>> searchUpdateSettings(
        utility::string_t indexUid,
        std::shared_ptr<Search_Settings> searchSettings
    ) const;

protected:
    std::shared_ptr<const ApiClient> m_ApiClient;
};

}
}

#endif /* HANZO_API_SettingsApi_H_ */

