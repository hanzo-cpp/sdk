/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * CollectionsApi.h
 *
 * 
 */

#ifndef HANZO_API_CollectionsApi_H_
#define HANZO_API_CollectionsApi_H_



#include "hanzo/ApiClient.h"

#include "hanzo/model/Vector_CreateCollectionRequest.h"
#include "hanzo/model/Vector_createCollection_200_response.h"
#include "hanzo/model/Vector_getCollection_200_response.h"
#include <cpprest/details/basic_types.h>
#include <boost/optional.hpp>

namespace hanzo {
namespace api {

using namespace hanzo::model;



class  CollectionsApi 
{
public:

    explicit CollectionsApi( std::shared_ptr<const ApiClient> apiClient );

    virtual ~CollectionsApi();

    /// <summary>
    /// The org&#39;s Base content types
    /// </summary>
    /// <remarks>
    /// Lists the content types in the org&#39;s managed Base, and creates one. This is what the console&#39;s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudDeleteV1Collections(
    ) const;
    /// <summary>
    /// One Base content type, and its records
    /// </summary>
    /// <remarks>
    /// Reads and writes below the collections root: &#x60;meta/scaffolds&#x60; is the field-template palette a new content type is built from, &#x60;&lt;name&gt;&#x60; is one content type (view, update, delete), &#x60;&lt;name&gt;/records&#x60; is that type&#39;s rows (list, create) and &#x60;&lt;name&gt;/records/&lt;id&gt;&#x60; is one row (get, update, delete). This is the data plane behind the console&#39;s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudDeleteV1CollectionsByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// The org&#39;s Base content types
    /// </summary>
    /// <remarks>
    /// Lists the content types in the org&#39;s managed Base, and creates one. This is what the console&#39;s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudGetV1Collections(
    ) const;
    /// <summary>
    /// One Base content type, and its records
    /// </summary>
    /// <remarks>
    /// Reads and writes below the collections root: &#x60;meta/scaffolds&#x60; is the field-template palette a new content type is built from, &#x60;&lt;name&gt;&#x60; is one content type (view, update, delete), &#x60;&lt;name&gt;/records&#x60; is that type&#39;s rows (list, create) and &#x60;&lt;name&gt;/records/&lt;id&gt;&#x60; is one row (get, update, delete). This is the data plane behind the console&#39;s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudGetV1CollectionsByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// The org&#39;s Base content types
    /// </summary>
    /// <remarks>
    /// Lists the content types in the org&#39;s managed Base, and creates one. This is what the console&#39;s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudOptionsV1Collections(
    ) const;
    /// <summary>
    /// One Base content type, and its records
    /// </summary>
    /// <remarks>
    /// Reads and writes below the collections root: &#x60;meta/scaffolds&#x60; is the field-template palette a new content type is built from, &#x60;&lt;name&gt;&#x60; is one content type (view, update, delete), &#x60;&lt;name&gt;/records&#x60; is that type&#39;s rows (list, create) and &#x60;&lt;name&gt;/records/&lt;id&gt;&#x60; is one row (get, update, delete). This is the data plane behind the console&#39;s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudOptionsV1CollectionsByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// The org&#39;s Base content types
    /// </summary>
    /// <remarks>
    /// Lists the content types in the org&#39;s managed Base, and creates one. This is what the console&#39;s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudPatchV1Collections(
    ) const;
    /// <summary>
    /// One Base content type, and its records
    /// </summary>
    /// <remarks>
    /// Reads and writes below the collections root: &#x60;meta/scaffolds&#x60; is the field-template palette a new content type is built from, &#x60;&lt;name&gt;&#x60; is one content type (view, update, delete), &#x60;&lt;name&gt;/records&#x60; is that type&#39;s rows (list, create) and &#x60;&lt;name&gt;/records/&lt;id&gt;&#x60; is one row (get, update, delete). This is the data plane behind the console&#39;s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudPatchV1CollectionsByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// The org&#39;s Base content types
    /// </summary>
    /// <remarks>
    /// Lists the content types in the org&#39;s managed Base, and creates one. This is what the console&#39;s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudPostV1Collections(
    ) const;
    /// <summary>
    /// One Base content type, and its records
    /// </summary>
    /// <remarks>
    /// Reads and writes below the collections root: &#x60;meta/scaffolds&#x60; is the field-template palette a new content type is built from, &#x60;&lt;name&gt;&#x60; is one content type (view, update, delete), &#x60;&lt;name&gt;/records&#x60; is that type&#39;s rows (list, create) and &#x60;&lt;name&gt;/records/&lt;id&gt;&#x60; is one row (get, update, delete). This is the data plane behind the console&#39;s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudPostV1CollectionsByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// The org&#39;s Base content types
    /// </summary>
    /// <remarks>
    /// Lists the content types in the org&#39;s managed Base, and creates one. This is what the console&#39;s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudPutV1Collections(
    ) const;
    /// <summary>
    /// One Base content type, and its records
    /// </summary>
    /// <remarks>
    /// Reads and writes below the collections root: &#x60;meta/scaffolds&#x60; is the field-template palette a new content type is built from, &#x60;&lt;name&gt;&#x60; is one content type (view, update, delete), &#x60;&lt;name&gt;/records&#x60; is that type&#39;s rows (list, create) and &#x60;&lt;name&gt;/records/&lt;id&gt;&#x60; is one row (get, update, delete). This is the data plane behind the console&#39;s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base&#39;s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller&#39;s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token&#39;s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller&#39;s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudPutV1CollectionsByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// Create collection
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="collectionName"></param>
    /// <param name="vectorCreateCollectionRequest"></param>
    pplx::task<std::shared_ptr<Vector_createCollection_200_response>> vectorCreateCollection(
        utility::string_t collectionName,
        std::shared_ptr<Vector_CreateCollectionRequest> vectorCreateCollectionRequest
    ) const;
    /// <summary>
    /// Delete collection
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="collectionName"></param>
    pplx::task<std::shared_ptr<Vector_createCollection_200_response>> vectorDeleteCollection(
        utility::string_t collectionName
    ) const;
    /// <summary>
    /// Get collection info
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="collectionName"></param>
    pplx::task<std::shared_ptr<Vector_getCollection_200_response>> vectorGetCollection(
        utility::string_t collectionName
    ) const;

protected:
    std::shared_ptr<const ApiClient> m_ApiClient;
};

}
}

#endif /* HANZO_API_CollectionsApi_H_ */

