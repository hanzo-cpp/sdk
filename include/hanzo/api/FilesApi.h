/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * FilesApi.h
 *
 * 
 */

#ifndef HANZO_API_FilesApi_H_
#define HANZO_API_FilesApi_H_



#include "hanzo/ApiClient.h"

#include "hanzo/model/Ai_Envelope.h"
#include "hanzo/model/Ai_Error.h"
#include "hanzo/model/Exec_listSessionFiles_200_response.h"
#include "hanzo/HttpContent.h"
#include "hanzo/model/Inline_object.h"
#include "hanzo/Object.h"
#include <cpprest/details/basic_types.h>
#include <boost/optional.hpp>

namespace hanzo {
namespace api {

using namespace hanzo::model;



class  FilesApi 
{
public:

    explicit FilesApi( std::shared_ptr<const ApiClient> apiClient );

    virtual ~FilesApi();

    /// <summary>
    /// Activate (file)
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="body"></param>
    pplx::task<std::shared_ptr<Ai_Envelope>> aiActivateFile(
        std::shared_ptr<Object> body
    ) const;
    /// <summary>
    /// Create a file
    /// </summary>
    /// <remarks>
    /// Create one file.
    /// </remarks>
    /// <param name="body"></param>
    pplx::task<std::shared_ptr<Ai_Envelope>> aiAddFile(
        std::shared_ptr<Object> body
    ) const;
    /// <summary>
    /// Delete a file
    /// </summary>
    /// <remarks>
    /// Delete one file.
    /// </remarks>
    /// <param name="owner">Owning organization.</param>
    /// <param name="name">Resource name, unique within the owner.</param>
    pplx::task<std::shared_ptr<Ai_Envelope>> aiDeleteFile(
        utility::string_t owner,
        utility::string_t name
    ) const;
    /// <summary>
    /// Active (file)
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    pplx::task<std::shared_ptr<Ai_Envelope>> aiGetActiveFile(
    ) const;
    /// <summary>
    /// Retrieve a file
    /// </summary>
    /// <remarks>
    /// Read one file by its (owner, name) key.
    /// </remarks>
    /// <param name="owner">Owning organization.</param>
    /// <param name="name">Resource name, unique within the owner.</param>
    pplx::task<std::shared_ptr<Ai_Envelope>> aiGetFileMy(
        utility::string_t owner,
        utility::string_t name
    ) const;
    /// <summary>
    /// List files
    /// </summary>
    /// <remarks>
    /// List the caller&#39;s files.
    /// </remarks>
    pplx::task<std::shared_ptr<Ai_Envelope>> aiGetFiles(
    ) const;
    /// <summary>
    /// List files across tenants
    /// </summary>
    /// <remarks>
    /// Cross-tenant listing. Admin-only; a tenant caller is refused.
    /// </remarks>
    pplx::task<std::shared_ptr<Ai_Envelope>> aiGetGlobalFiles(
    ) const;
    /// <summary>
    /// Vectors (file)
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="owner">Owning organization.</param>
    /// <param name="name">Resource name, unique within the owner.</param>
    /// <param name="body"></param>
    pplx::task<std::shared_ptr<Ai_Envelope>> aiRefreshFileVectors(
        utility::string_t owner,
        utility::string_t name,
        std::shared_ptr<Object> body
    ) const;
    /// <summary>
    /// Replace a file
    /// </summary>
    /// <remarks>
    /// Identical to PATCH — the handler takes a whole object either way.
    /// </remarks>
    /// <param name="owner">Owning organization.</param>
    /// <param name="name">Resource name, unique within the owner.</param>
    /// <param name="body"></param>
    pplx::task<std::shared_ptr<Ai_Envelope>> aiReplaceFile(
        utility::string_t owner,
        utility::string_t name,
        std::shared_ptr<Object> body
    ) const;
    /// <summary>
    /// Update a file
    /// </summary>
    /// <remarks>
    /// Update one file. PATCH and PUT reach the same handler, which has always taken a whole object.
    /// </remarks>
    /// <param name="owner">Owning organization.</param>
    /// <param name="name">Resource name, unique within the owner.</param>
    /// <param name="body"></param>
    pplx::task<std::shared_ptr<Ai_Envelope>> aiUpdateFile(
        utility::string_t owner,
        utility::string_t name,
        std::shared_ptr<Object> body
    ) const;
    /// <summary>
    /// Upload (file)
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="body"></param>
    pplx::task<std::shared_ptr<Ai_Envelope>> aiUploadFile(
        std::shared_ptr<Object> body
    ) const;
    /// <summary>
    /// The session file surface
    /// </summary>
    /// <remarks>
    /// The root of the executor&#39;s session-file surface. The contract addresses a session&#39;s files one segment down (/v1/files/{session_id}); this bare address is served because one registration owns the whole prefix, and what it answers is the executor&#39;s to decide.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudDeleteV1Files(
    ) const;
    /// <summary>
    /// List the files in an execution session
    /// </summary>
    /// <remarks>
    /// Lists what a session holds, addressed by session id — the uploads a run can read and the artifacts it produced, each then fetched from /v1/download. The listing&#39;s shape is the executor&#39;s own; this module names only that the address is keyed by session id.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudDeleteV1FilesByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// The session file surface
    /// </summary>
    /// <remarks>
    /// The root of the executor&#39;s session-file surface. The contract addresses a session&#39;s files one segment down (/v1/files/{session_id}); this bare address is served because one registration owns the whole prefix, and what it answers is the executor&#39;s to decide.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudGetV1Files(
    ) const;
    /// <summary>
    /// List the files in an execution session
    /// </summary>
    /// <remarks>
    /// Lists what a session holds, addressed by session id — the uploads a run can read and the artifacts it produced, each then fetched from /v1/download. The listing&#39;s shape is the executor&#39;s own; this module names only that the address is keyed by session id.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudGetV1FilesByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// The session file surface
    /// </summary>
    /// <remarks>
    /// The root of the executor&#39;s session-file surface. The contract addresses a session&#39;s files one segment down (/v1/files/{session_id}); this bare address is served because one registration owns the whole prefix, and what it answers is the executor&#39;s to decide.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudOptionsV1Files(
    ) const;
    /// <summary>
    /// List the files in an execution session
    /// </summary>
    /// <remarks>
    /// Lists what a session holds, addressed by session id — the uploads a run can read and the artifacts it produced, each then fetched from /v1/download. The listing&#39;s shape is the executor&#39;s own; this module names only that the address is keyed by session id.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudOptionsV1FilesByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// The session file surface
    /// </summary>
    /// <remarks>
    /// The root of the executor&#39;s session-file surface. The contract addresses a session&#39;s files one segment down (/v1/files/{session_id}); this bare address is served because one registration owns the whole prefix, and what it answers is the executor&#39;s to decide.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudPatchV1Files(
    ) const;
    /// <summary>
    /// List the files in an execution session
    /// </summary>
    /// <remarks>
    /// Lists what a session holds, addressed by session id — the uploads a run can read and the artifacts it produced, each then fetched from /v1/download. The listing&#39;s shape is the executor&#39;s own; this module names only that the address is keyed by session id.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudPatchV1FilesByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// The session file surface
    /// </summary>
    /// <remarks>
    /// The root of the executor&#39;s session-file surface. The contract addresses a session&#39;s files one segment down (/v1/files/{session_id}); this bare address is served because one registration owns the whole prefix, and what it answers is the executor&#39;s to decide.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudPostV1Files(
    ) const;
    /// <summary>
    /// List the files in an execution session
    /// </summary>
    /// <remarks>
    /// Lists what a session holds, addressed by session id — the uploads a run can read and the artifacts it produced, each then fetched from /v1/download. The listing&#39;s shape is the executor&#39;s own; this module names only that the address is keyed by session id.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudPostV1FilesByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// The session file surface
    /// </summary>
    /// <remarks>
    /// The root of the executor&#39;s session-file surface. The contract addresses a session&#39;s files one segment down (/v1/files/{session_id}); this bare address is served because one registration owns the whole prefix, and what it answers is the executor&#39;s to decide.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    pplx::task<void> cloudPutV1Files(
    ) const;
    /// <summary>
    /// List the files in an execution session
    /// </summary>
    /// <remarks>
    /// Lists what a session holds, addressed by session id — the uploads a run can read and the artifacts it produced, each then fetched from /v1/download. The listing&#39;s shape is the executor&#39;s own; this module names only that the address is keyed by session id.  NOTHING RUNS HERE. cloud forwards the request to the sandboxed executor byte for byte and forwards its answer back the same way — the status, the Content-Type and every field are the executor&#39;s, including fields this repo has never named and including its own 4xx. There is no os/exec anywhere in this process: the sandbox is the isolation boundary, and cloud adds only the credential check and the single public address.  AUTH is a shared SERVICE key on X-API-Key, compared in constant time — not a user JWT. The chat server calls this server-side on a user&#39;s behalf, so this surface carries no org scope and no per-user identity; separation between callers is the executor&#39;s session, not this edge&#39;s. A wrong key is 401, and a deployment with no key configured is 503 rather than open.  One registration owns this address for every method, so which methods actually answer is the executor&#39;s decision, not this edge&#39;s.
    /// </remarks>
    /// <param name="wildcard1"></param>
    pplx::task<void> cloudPutV1FilesByWildcard1(
        utility::string_t wildcard1
    ) const;
    /// <summary>
    /// Download a produced file by id
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="id"></param>
    pplx::task<std::shared_ptr<HttpContent>> execDownloadFile(
        utility::string_t id
    ) const;
    /// <summary>
    /// List the files produced in a session
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="sessionId"></param>
    pplx::task<std::shared_ptr<Exec_listSessionFiles_200_response>> execListSessionFiles(
        utility::string_t sessionId
    ) const;

protected:
    std::shared_ptr<const ApiClient> m_ApiClient;
};

}
}

#endif /* HANZO_API_FilesApi_H_ */

