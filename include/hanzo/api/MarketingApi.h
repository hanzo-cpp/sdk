/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * MarketingApi.h
 *
 * 
 */

#ifndef HANZO_API_MarketingApi_H_
#define HANZO_API_MarketingApi_H_



#include "hanzo/ApiClient.h"

#include "hanzo/model/Cloud_Audience.h"
#include "hanzo/model/Cloud_AudienceList.h"
#include "hanzo/model/Cloud_AudiencePreview.h"
#include "hanzo/model/Cloud_CalendarPost.h"
#include "hanzo/model/Cloud_Campaign.h"
#include "hanzo/model/Cloud_CampaignList.h"
#include "hanzo/model/Cloud_EnrollInput.h"
#include "hanzo/model/Cloud_EnrollResult.h"
#include "hanzo/model/Cloud_EnrollmentList.h"
#include "hanzo/model/Cloud_PostList.h"
#include "hanzo/model/Cloud_PromoList.h"
#include "hanzo/model/Cloud_Quote.h"
#include "hanzo/model/Cloud_RedeemInput.h"
#include "hanzo/model/Cloud_RedeemResult.h"
#include "hanzo/model/Cloud_Redemption.h"
#include "hanzo/model/Cloud_ScheduleInput.h"
#include "hanzo/model/Cloud_Sequence.h"
#include "hanzo/model/Cloud_SequenceList.h"
#include "hanzo/model/Cloud_SequenceStatus.h"
#include "hanzo/model/Cloud_SequenceView.h"
#include "hanzo/model/Cloud_Step.h"
#include "hanzo/model/Cloud_StepInput.h"
#include "hanzo/model/Cloud_StepList.h"
#include "hanzo/model/Cloud_Summary.h"
#include "hanzo/model/Cloud_Suppression.h"
#include "hanzo/model/Cloud_SuppressionList.h"
#include "hanzo/model/Cloud_Unsubscribed.h"
#include "hanzo/model/Commerce_Error.h"
#include "hanzo/Object.h"
#include <cpprest/details/basic_types.h>
#include <boost/optional.hpp>

namespace hanzo {
namespace api {

using namespace hanzo::model;



class  MarketingApi 
{
public:

    explicit MarketingApi( std::shared_ptr<const ApiClient> apiClient );

    virtual ~MarketingApi();

    /// <summary>
    /// Removes one of the caller org&#39;s audiences and answers 204.
    /// </summary>
    /// <remarks>
    /// Removes one of the caller org&#39;s audiences and answers 204. It deletes the saved filter only — no customer, event or enrollment is touched.
    /// </remarks>
    /// <param name="id">ID is the audience id from the path, as returned by create.</param>
    pplx::task<void> cloudDeleteV1MarketingAudiencesId(
        utility::string_t id
    ) const;
    /// <summary>
    /// Removes one of the caller org&#39;s posts and answers 204.
    /// </summary>
    /// <remarks>
    /// Removes one of the caller org&#39;s posts and answers 204. A post already published is deleted from the calendar only — nothing is retracted from the network it went out on.
    /// </remarks>
    /// <param name="id">ID is the post id from the path, as returned by create.</param>
    pplx::task<void> cloudDeleteV1MarketingCalendarId(
        utility::string_t id
    ) const;
    /// <summary>
    /// Removes one of the caller org&#39;s campaigns and answers 204.
    /// </summary>
    /// <remarks>
    /// Removes one of the caller org&#39;s campaigns and answers 204. A campaign belonging to another org reads as not found and is left untouched.
    /// </remarks>
    /// <param name="id">ID is the campaign id from the path, as returned by create.</param>
    pplx::task<void> cloudDeleteV1MarketingCampaignsId(
        utility::string_t id
    ) const;
    /// <summary>
    /// Re-subscribes an address on one channel and answers 204.
    /// </summary>
    /// <remarks>
    /// Re-subscribes an address on one channel and answers 204. An address that is not on the list reads as not found.
    /// </remarks>
    /// <param name="channel">Channel is the surface opted out of: email, sms, social, meta, google or tiktok. Empty means email. Opting out of one leaves the others reachable. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="address">Address is the recipient, normalized (lower-cased, trimmed) so an opt-out cannot be slipped past on a case or whitespace difference. Required. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="reason">Reason is a free-text note, capped at 1024 bytes. The public one-click endpoint records \&quot;one-click unsubscribe\&quot;. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="createdAt">CreatedAt is unix seconds, server-assigned. (optional, default to 0)</param>
    pplx::task<void> cloudDeleteV1MarketingSuppressions(
        boost::optional<utility::string_t> channel,
        boost::optional<utility::string_t> address,
        boost::optional<utility::string_t> reason,
        boost::optional<int32_t> createdAt
    ) const;
    /// <summary>
    /// Returns the org&#39;s saved audiences, most recently updated first.
    /// </summary>
    /// <remarks>
    /// Returns the org&#39;s saved audiences, most recently updated first.
    /// </remarks>
    /// <param name="limit">Limit caps the rows returned; 0 means 200 and nothing above 1000 is honoured. (optional, default to 0)</param>
    pplx::task<std::shared_ptr<Cloud_AudienceList>> cloudGetV1MarketingAudiences(
        boost::optional<int32_t> limit
    ) const;
    /// <summary>
    /// Returns one of the caller org&#39;s saved audiences.
    /// </summary>
    /// <remarks>
    /// Returns one of the caller org&#39;s saved audiences. An audience belonging to another org reads as not found.
    /// </remarks>
    /// <param name="id">ID is the audience id from the path, as returned by create.</param>
    pplx::task<std::shared_ptr<Cloud_Audience>> cloudGetV1MarketingAudiencesId(
        utility::string_t id
    ) const;
    /// <summary>
    /// Evaluates the cohort LIVE — the same resolution an enrollment would run — and reports how big it is and how many real mailboxes it reaches.
    /// </summary>
    /// <remarks>
    /// Evaluates the cohort LIVE — the same resolution an enrollment would run — and reports how big it is and how many real mailboxes it reaches. It is the honest answer to \&quot;is this send worth making\&quot;: a cohort of 500 that mails 3 says so, in deliverable and unmatched. Nothing is sent.
    /// </remarks>
    /// <param name="id">ID is the audience id from the path, as returned by create.</param>
    pplx::task<std::shared_ptr<Cloud_AudiencePreview>> cloudGetV1MarketingAudiencesIdPreview(
        utility::string_t id
    ) const;
    /// <summary>
    /// Returns the org&#39;s calendar, soonest scheduled first, optionally narrowed to one status.
    /// </summary>
    /// <remarks>
    /// Returns the org&#39;s calendar, soonest scheduled first, optionally narrowed to one status.
    /// </remarks>
    /// <param name="status">Status keeps only posts in that state (draft, scheduled, published, failed, canceled). Empty means every post. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="limit">Limit caps the rows returned; 0 means 200 and nothing above 1000 is honoured. (optional, default to 0)</param>
    pplx::task<std::shared_ptr<Cloud_PostList>> cloudGetV1MarketingCalendar(
        boost::optional<utility::string_t> status,
        boost::optional<int32_t> limit
    ) const;
    /// <summary>
    /// Returns one of the caller org&#39;s posts, including the exact error behind a failed publish.
    /// </summary>
    /// <remarks>
    /// Returns one of the caller org&#39;s posts, including the exact error behind a failed publish. A post belonging to another org reads as not found.
    /// </remarks>
    /// <param name="id">ID is the post id from the path, as returned by create.</param>
    pplx::task<std::shared_ptr<Cloud_CalendarPost>> cloudGetV1MarketingCalendarId(
        utility::string_t id
    ) const;
    /// <summary>
    /// Returns the org&#39;s campaigns, most recently updated first, optionally narrowed to one lifecycle status.
    /// </summary>
    /// <remarks>
    /// Returns the org&#39;s campaigns, most recently updated first, optionally narrowed to one lifecycle status.
    /// </remarks>
    /// <param name="status">Status keeps only campaigns in that lifecycle state (draft, scheduled, active, paused, completed). Empty means every campaign. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="limit">Limit caps the rows returned; 0 means 200 and nothing above 1000 is honoured. (optional, default to 0)</param>
    pplx::task<std::shared_ptr<Cloud_CampaignList>> cloudGetV1MarketingCampaigns(
        boost::optional<utility::string_t> status,
        boost::optional<int32_t> limit
    ) const;
    /// <summary>
    /// Returns one of the caller org&#39;s campaigns.
    /// </summary>
    /// <remarks>
    /// Returns one of the caller org&#39;s campaigns. A campaign belonging to another org reads as not found.
    /// </remarks>
    /// <param name="id">ID is the campaign id from the path, as returned by create.</param>
    pplx::task<std::shared_ptr<Cloud_Campaign>> cloudGetV1MarketingCampaignsId(
        utility::string_t id
    ) const;
    /// <summary>
    /// Returns every promo the deployment offers with its live counters: how many orgs have redeemed it and how many redemptions remain under the cap.
    /// </summary>
    /// <remarks>
    /// Returns every promo the deployment offers with its live counters: how many orgs have redeemed it and how many redemptions remain under the cap. The promos are fleet-wide, not per-org — only the counters move.
    /// </remarks>
    pplx::task<std::shared_ptr<Cloud_PromoList>> cloudGetV1MarketingPromos(
    ) const;
    /// <summary>
    /// Prices a promo against a plan and seat count.
    /// </summary>
    /// <remarks>
    /// Prices a promo against a plan and seat count. It is PURE: nothing is redeemed, credited or counted, so it is safe to call from a pricing page on every keystroke. An inactive promo or an exhausted cap quotes ineligible with the reason rather than erroring.
    /// </remarks>
    /// <param name="code">Code is the promo code from the path.</param>
    /// <param name="plan">Plan is the plan being priced: pro, max or team. Anything else (including the free Developer plan) has no list price and so nothing to discount. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="seats">Seats is the Team seat count; 0 means 1, and it is ignored for the single-seat plans. (optional, default to 0)</param>
    pplx::task<std::shared_ptr<Cloud_Quote>> cloudGetV1MarketingPromosCodeEligibility(
        utility::string_t code,
        boost::optional<utility::string_t> plan,
        boost::optional<int32_t> seats
    ) const;
    /// <summary>
    /// Returns the caller org&#39;s OWN redemption of a promo — an org-scoped read, so it can never surface another tenant&#39;s.
    /// </summary>
    /// <remarks>
    /// Returns the caller org&#39;s OWN redemption of a promo — an org-scoped read, so it can never surface another tenant&#39;s. Not found when this org has not redeemed it.
    /// </remarks>
    /// <param name="code">Code is the promo code from the path, e.g. \&quot;first1000\&quot;.</param>
    pplx::task<std::shared_ptr<Cloud_Redemption>> cloudGetV1MarketingPromosCodeRedemption(
        utility::string_t code
    ) const;
    /// <summary>
    /// Returns the org&#39;s drip sequences, most recently updated first.
    /// </summary>
    /// <remarks>
    /// Returns the org&#39;s drip sequences, most recently updated first.
    /// </remarks>
    /// <param name="limit">Limit caps the rows returned; 0 means 200 and nothing above 1000 is honoured. (optional, default to 0)</param>
    pplx::task<std::shared_ptr<Cloud_SequenceList>> cloudGetV1MarketingSequences(
        boost::optional<int32_t> limit
    ) const;
    /// <summary>
    /// Returns one of the caller org&#39;s sequences together with its steps in send order.
    /// </summary>
    /// <remarks>
    /// Returns one of the caller org&#39;s sequences together with its steps in send order. A sequence belonging to another org reads as not found.
    /// </remarks>
    /// <param name="id">ID is the sequence id from the path, as returned by create.</param>
    pplx::task<std::shared_ptr<Cloud_SequenceView>> cloudGetV1MarketingSequencesId(
        utility::string_t id
    ) const;
    /// <summary>
    /// Returns who is walking one sequence, most recently enrolled first, with each walk&#39;s current step and next due time.
    /// </summary>
    /// <remarks>
    /// Returns who is walking one sequence, most recently enrolled first, with each walk&#39;s current step and next due time.
    /// </remarks>
    /// <param name="id">ID is the sequence id from the path.</param>
    /// <param name="limit">Limit caps the rows returned; 0 means 200 and nothing above 1000 is honoured. (optional, default to 0)</param>
    pplx::task<std::shared_ptr<Cloud_EnrollmentList>> cloudGetV1MarketingSequencesIdEnrollments(
        utility::string_t id,
        boost::optional<int32_t> limit
    ) const;
    /// <summary>
    /// Returns one sequence&#39;s steps in send order.
    /// </summary>
    /// <remarks>
    /// Returns one sequence&#39;s steps in send order.
    /// </remarks>
    /// <param name="id">ID is the sequence id from the path, as returned by create.</param>
    pplx::task<std::shared_ptr<Cloud_StepList>> cloudGetV1MarketingSequencesIdSteps(
        utility::string_t id
    ) const;
    /// <summary>
    /// Rolls up the caller org&#39;s campaigns: how many there are, how many are active, and the summed budget and spend in cents.
    /// </summary>
    /// <remarks>
    /// Rolls up the caller org&#39;s campaigns: how many there are, how many are active, and the summed budget and spend in cents.
    /// </remarks>
    pplx::task<std::shared_ptr<Cloud_Summary>> cloudGetV1MarketingSummary(
    ) const;
    /// <summary>
    /// Returns the org&#39;s opt-out list, newest first — everyone the send gate will refuse to deliver to.
    /// </summary>
    /// <remarks>
    /// Returns the org&#39;s opt-out list, newest first — everyone the send gate will refuse to deliver to.
    /// </remarks>
    /// <param name="limit">Limit caps the rows returned; 0 means 200 and nothing above 1000 is honoured. (optional, default to 0)</param>
    pplx::task<std::shared_ptr<Cloud_SuppressionList>> cloudGetV1MarketingSuppressions(
        boost::optional<int32_t> limit
    ) const;
    /// <summary>
    /// Is the PUBLIC one-click endpoint (no principal): a recipient clicks the signed link in an email footer.
    /// </summary>
    /// <remarks>
    /// Is the PUBLIC one-click endpoint (no principal): a recipient clicks the signed link in an email footer. The token binds (org, channel, address), so a caller can only opt OUT exactly the tuple it was minted for — never another address and never another org. An invalid token is refused, and a deployment with no KMS-sealed key refuses rather than accepting anything.
    /// </remarks>
    /// <param name="org">Org is the org the link was minted for. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="channel">Channel is the surface to opt out of. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="address">Address is the recipient to opt out. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="token">Token is the HMAC over (org, channel, address). It is the ONLY authority here — there is no principal — so it binds the request to one tuple and nothing else. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    pplx::task<std::shared_ptr<Cloud_Unsubscribed>> cloudGetV1MarketingUnsubscribe(
        boost::optional<utility::string_t> org,
        boost::optional<utility::string_t> channel,
        boost::optional<utility::string_t> address,
        boost::optional<utility::string_t> token
    ) const;
    /// <summary>
    /// Saves a cohort filter for the caller&#39;s org.
    /// </summary>
    /// <remarks>
    /// Saves a cohort filter for the caller&#39;s org. Name is required. Omitting event saves the WHOLE-ORG audience — every mailable customer — which needs no analytics warehouse; naming one narrows that roster to the customers who fired it within windowDays.
    /// </remarks>
    /// <param name="cloudAudience"></param>
    pplx::task<std::shared_ptr<Cloud_Audience>> cloudPostV1MarketingAudiences(
        std::shared_ptr<Cloud_Audience> cloudAudience
    ) const;
    /// <summary>
    /// Adds a post to the content calendar.
    /// </summary>
    /// <remarks>
    /// Adds a post to the content calendar. Channel and body are required. A scheduledAt in the future makes the post \&quot;scheduled\&quot; and the durable sweep publishes it when it comes due — claimed once, so a post publishes at most once; without one it stays a draft.
    /// </remarks>
    /// <param name="cloudCalendarPost"></param>
    pplx::task<std::shared_ptr<Cloud_CalendarPost>> cloudPostV1MarketingCalendar(
        std::shared_ptr<Cloud_CalendarPost> cloudCalendarPost
    ) const;
    /// <summary>
    /// Publishes a post NOW, synchronously, whatever its schedule.
    /// </summary>
    /// <remarks>
    /// Publishes a post NOW, synchronously, whatever its schedule. No social connector is wired today, so every channel answers an honest 501 naming the seam a real one would plug into, and the post is recorded failed with that exact reason — never a faked \&quot;published\&quot;.
    /// </remarks>
    /// <param name="id">ID is the post id from the path, as returned by create.</param>
    pplx::task<std::shared_ptr<Cloud_CalendarPost>> cloudPostV1MarketingCalendarIdPublish(
        utility::string_t id
    ) const;
    /// <summary>
    /// Registers a campaign in the caller&#39;s org.
    /// </summary>
    /// <remarks>
    /// Registers a campaign in the caller&#39;s org. Name is required; channel defaults to email and status to draft, and a future scheduledAt with no explicit status makes the campaign \&quot;scheduled\&quot;. Budget and spend are cents and are clamped to &gt;&#x3D; 0. The id, createdAt and updatedAt of the input are ignored — the server assigns them.
    /// </remarks>
    /// <param name="cloudCampaign"></param>
    pplx::task<std::shared_ptr<Cloud_Campaign>> cloudPostV1MarketingCampaigns(
        std::shared_ptr<Cloud_Campaign> cloudCampaign
    ) const;
    /// <summary>
    /// Sets a campaign&#39;s send time and moves it to \&quot;scheduled\&quot;.
    /// </summary>
    /// <remarks>
    /// Sets a campaign&#39;s send time and moves it to \&quot;scheduled\&quot;. A scheduledAt of 0 clears the schedule and returns it to \&quot;draft\&quot;.
    /// </remarks>
    /// <param name="id">ID is the campaign id from the path.</param>
    /// <param name="cloudScheduleInput"></param>
    pplx::task<std::shared_ptr<Cloud_Campaign>> cloudPostV1MarketingCampaignsIdSchedule(
        utility::string_t id,
        std::shared_ptr<Cloud_ScheduleInput> cloudScheduleInput
    ) const;
    /// <summary>
    /// Redeems the promo for the caller&#39;s org, crediting the discount value to its wallet through the finance ledger.
    /// </summary>
    /// <remarks>
    /// Redeems the promo for the caller&#39;s org, crediting the discount value to its wallet through the finance ledger. Three guards run under one lock so the cap cannot be raced past: the fleet-wide redemption cap, one redemption per org, and one per payment instrument.  It is IDEMPOTENT: an org that already redeemed gets its original redemption back with alreadyRedeemed true and is not credited twice.
    /// </remarks>
    /// <param name="code">Code is the promo code from the path.</param>
    /// <param name="cloudRedeemInput"></param>
    pplx::task<std::shared_ptr<Cloud_RedeemResult>> cloudPostV1MarketingPromosCodeRedeem(
        utility::string_t code,
        std::shared_ptr<Cloud_RedeemInput> cloudRedeemInput
    ) const;
    /// <summary>
    /// Registers a drip sequence in the caller&#39;s org.
    /// </summary>
    /// <remarks>
    /// Registers a drip sequence in the caller&#39;s org. Name is required; status defaults to draft, and a sequence must be ACTIVE before it will accept enrollments. The id, createdAt and updatedAt of the input are ignored — the server assigns them.
    /// </remarks>
    /// <param name="cloudSequence"></param>
    pplx::task<std::shared_ptr<Cloud_Sequence>> cloudPostV1MarketingSequences(
        std::shared_ptr<Cloud_Sequence> cloudSequence
    ) const;
    /// <summary>
    /// Adds one contact or a whole audience to a sequence and schedules the first step for each.
    /// </summary>
    /// <remarks>
    /// Adds one contact or a whole audience to a sequence and schedules the first step for each. The sequence must be ACTIVE (a draft sends nothing), and the request must name exactly one of address or audienceId.  Enrolling is ALL this does: the message itself is sent later by the drip engine, through the suppression gate, so an opted-out customer can be enrolled here and still never be mailed. Re-posting is safe — an address this sequence already took is counted in alreadyEnrolled and never double-dripped — which is what makes retrying a partially-applied announcement a resume rather than a second send.
    /// </remarks>
    /// <param name="id">ID is the sequence id from the path.</param>
    /// <param name="cloudEnrollInput"></param>
    pplx::task<std::shared_ptr<Cloud_EnrollResult>> cloudPostV1MarketingSequencesIdEnroll(
        utility::string_t id,
        std::shared_ptr<Cloud_EnrollInput> cloudEnrollInput
    ) const;
    /// <summary>
    /// Stops one walk mid-sequence and answers 204: no further step is sent, and steps already delivered are not recalled.
    /// </summary>
    /// <remarks>
    /// Stops one walk mid-sequence and answers 204: no further step is sent, and steps already delivered are not recalled. Only an ACTIVE enrollment can be canceled — one already completed or canceled reads as not found.
    /// </remarks>
    /// <param name="id">ID is the sequence id from the path.</param>
    /// <param name="eid">EID is the enrollment id from the path, as returned by a single-address enroll.</param>
    pplx::task<void> cloudPostV1MarketingSequencesIdEnrollmentsEidCancel(
        utility::string_t id,
        utility::string_t eid
    ) const;
    /// <summary>
    /// Flips draft/active/archived — the activation gate for sending, since only an active sequence accepts enrollments.
    /// </summary>
    /// <remarks>
    /// Flips draft/active/archived — the activation gate for sending, since only an active sequence accepts enrollments. It does not touch enrollments already walking: archiving stops new ones, not in-flight ones.
    /// </remarks>
    /// <param name="id">ID is the sequence id from the path.</param>
    /// <param name="cloudSequenceStatus"></param>
    pplx::task<std::shared_ptr<Cloud_SequenceStatus>> cloudPostV1MarketingSequencesIdStatus(
        utility::string_t id,
        std::shared_ptr<Cloud_SequenceStatus> cloudSequenceStatus
    ) const;
    /// <summary>
    /// Appends a message to the END of a sequence: the new step&#39;s idx is one past the last, so steps arrive in the order they are added.
    /// </summary>
    /// <remarks>
    /// Appends a message to the END of a sequence: the new step&#39;s idx is one past the last, so steps arrive in the order they are added. Body is required and delaySeconds must be &gt;&#x3D; 0. Adding a step does not disturb enrollments already walking — one that has passed this index simply never sees it.
    /// </remarks>
    /// <param name="id">SequenceID is the sequence id from the path (the route&#39;s :id).</param>
    /// <param name="cloudStepInput"></param>
    pplx::task<std::shared_ptr<Cloud_Step>> cloudPostV1MarketingSequencesIdSteps(
        utility::string_t id,
        std::shared_ptr<Cloud_StepInput> cloudStepInput
    ) const;
    /// <summary>
    /// Records an opt-out for the org (admin / self-service management).
    /// </summary>
    /// <remarks>
    /// Records an opt-out for the org (admin / self-service management). Address is required; channel defaults to email. It is idempotent: re-suppressing the same tuple keeps the original record rather than erroring. From here on the ONE send gate refuses that recipient on that channel.
    /// </remarks>
    /// <param name="cloudSuppression"></param>
    pplx::task<std::shared_ptr<Cloud_Suppression>> cloudPostV1MarketingSuppressions(
        std::shared_ptr<Cloud_Suppression> cloudSuppression
    ) const;
    /// <summary>
    /// Replaces a post&#39;s editable fields.
    /// </summary>
    /// <remarks>
    /// Replaces a post&#39;s editable fields. It is a full write, not a patch, and it RESETS the lifecycle from the schedule: a scheduledAt makes the post \&quot;scheduled\&quot; again and none makes it a draft — so editing a failed post requeues it rather than leaving it stuck.
    /// </remarks>
    /// <param name="id">ID is the server-assigned post id (\&quot;cal_\&quot; + 128 random bits).</param>
    /// <param name="cloudCalendarPost"></param>
    pplx::task<std::shared_ptr<Cloud_CalendarPost>> cloudPutV1MarketingCalendarId(
        utility::string_t id,
        std::shared_ptr<Cloud_CalendarPost> cloudCalendarPost
    ) const;
    /// <summary>
    /// Replaces a campaign&#39;s editable fields.
    /// </summary>
    /// <remarks>
    /// Replaces a campaign&#39;s editable fields. It is a full write, not a patch: every field takes the value in the body, and an omitted one is cleared. The id comes from the path — the body cannot retarget another campaign — and createdAt is never rewritten.
    /// </remarks>
    /// <param name="id">ID is the server-assigned campaign id (\&quot;camp_\&quot; + 128 random bits).</param>
    /// <param name="cloudCampaign"></param>
    pplx::task<std::shared_ptr<Cloud_Campaign>> cloudPutV1MarketingCampaignsId(
        utility::string_t id,
        std::shared_ptr<Cloud_Campaign> cloudCampaign
    ) const;
    /// <summary>
    /// Create marketing campaign
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="body"></param>
    pplx::task<std::shared_ptr<Object>> commerceCreateMarketingCampaign(
        std::shared_ptr<Object> body
    ) const;

protected:
    std::shared_ptr<const ApiClient> m_ApiClient;
};

}
}

#endif /* HANZO_API_MarketingApi_H_ */

