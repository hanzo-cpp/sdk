/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * PubSubApi.h
 *
 * 
 */

#ifndef HANZO_API_PubSubApi_H_
#define HANZO_API_PubSubApi_H_



#include "hanzo/ApiClient.h"

#include "hanzo/model/Cloud_bucketRecord.h"
#include "hanzo/model/Cloud_bucketWrite.h"
#include "hanzo/model/Cloud_busAck.h"
#include "hanzo/model/Cloud_busMessage.h"
#include "hanzo/model/Cloud_busPublish.h"
#include "hanzo/model/Cloud_busRequest.h"
#include "hanzo/model/Cloud_consumerPage.h"
#include "hanzo/model/Cloud_consumerRecord.h"
#include "hanzo/model/Cloud_consumerWrite.h"
#include "hanzo/model/Cloud_fetchQuery.h"
#include "hanzo/model/Cloud_kvAck.h"
#include "hanzo/model/Cloud_kvEntry.h"
#include "hanzo/model/Cloud_kvPage.h"
#include "hanzo/model/Cloud_kvWrite.h"
#include "hanzo/model/Cloud_messagePage.h"
#include "hanzo/model/Cloud_streamPage.h"
#include "hanzo/model/Cloud_streamRecord.h"
#include "hanzo/model/Cloud_streamUpdate.h"
#include "hanzo/model/Cloud_streamWrite.h"
#include "hanzo/model/Kv_listChannels_200_response.h"
#include "hanzo/model/Kv_publish_200_response.h"
#include "hanzo/model/Kv_publish_request.h"
#include <vector>
#include <cpprest/details/basic_types.h>
#include <boost/optional.hpp>

namespace hanzo {
namespace api {

using namespace hanzo::model;



class  PubSubApi 
{
public:

    explicit PubSubApi( std::shared_ptr<const ApiClient> apiClient );

    virtual ~PubSubApi();

    /// <summary>
    /// DeleteStream removes one stream of the caller&#39;s org — its retained messages and its consumers with it — and answers 204 with no body.
    /// </summary>
    /// <remarks>
    /// DeleteStream removes one stream of the caller&#39;s org — its retained messages and its consumers with it — and answers 204 with no body. 404 when the org has no stream of that name.
    /// </remarks>
    /// <param name="stream">Stream is the stream&#39;s name, from the path.</param>
    pplx::task<void> cloudDeleteV1PubsubJetstreamStreamsStream(
        utility::string_t stream
    ) const;
    /// <summary>
    /// DeleteConsumer removes one consumer — its cursor, not the stream&#39;s messages — and answers 204 with no body.
    /// </summary>
    /// <remarks>
    /// DeleteConsumer removes one consumer — its cursor, not the stream&#39;s messages — and answers 204 with no body. 404 when the stream or the consumer does not exist.
    /// </remarks>
    /// <param name="stream">Stream is the stream, from the path.</param>
    /// <param name="name">Name is the consumer, from the path.</param>
    pplx::task<void> cloudDeleteV1PubsubJetstreamStreamsStreamConsumersName(
        utility::string_t stream,
        utility::string_t name
    ) const;
    /// <summary>
    /// DeleteBucket removes one bucket of the caller&#39;s org — every key and every revision with it — and answers 204 with no body.
    /// </summary>
    /// <remarks>
    /// DeleteBucket removes one bucket of the caller&#39;s org — every key and every revision with it — and answers 204 with no body. 404 when the org has no bucket of that name.
    /// </remarks>
    /// <param name="bucket">Bucket is the bucket&#39;s name, from the path.</param>
    pplx::task<void> cloudDeleteV1PubsubKvBucket(
        utility::string_t bucket
    ) const;
    /// <summary>
    /// Delete removes one key — a delete marker in the key&#39;s history, so watchers see it and Get answers 404 — and answers 204 with no body.
    /// </summary>
    /// <remarks>
    /// Delete removes one key — a delete marker in the key&#39;s history, so watchers see it and Get answers 404 — and answers 204 with no body. 404 when the bucket does not exist.
    /// </remarks>
    /// <param name="bucket">Bucket is the bucket, from the path.</param>
    /// <param name="key">Key is the key, from the path.</param>
    pplx::task<void> cloudDeleteV1PubsubKvBucketKey(
        utility::string_t bucket,
        utility::string_t key
    ) const;
    /// <summary>
    /// ListStreams returns the org&#39;s streams, sorted by name.
    /// </summary>
    /// <remarks>
    /// ListStreams returns the org&#39;s streams, sorted by name.  A stream is the durable log: it captures every message published to its subjects and retains them by its own limits, independent of any consumer. The listing is org-scoped server-side — one org can never see another&#39;s streams, and the platform&#39;s own planes never appear.
    /// </remarks>
    pplx::task<std::shared_ptr<Cloud_streamPage>> cloudGetV1PubsubJetstreamStreams(
    ) const;
    /// <summary>
    /// GetStream returns one stream of the caller&#39;s org — its configuration and its live state (messages, bytes, sequence range, consumer count).
    /// </summary>
    /// <remarks>
    /// GetStream returns one stream of the caller&#39;s org — its configuration and its live state (messages, bytes, sequence range, consumer count). 404 when the org has no stream of that name.
    /// </remarks>
    /// <param name="stream">Stream is the stream&#39;s name, from the path.</param>
    pplx::task<std::shared_ptr<Cloud_streamRecord>> cloudGetV1PubsubJetstreamStreamsStream(
        utility::string_t stream
    ) const;
    /// <summary>
    /// ListConsumers returns one stream&#39;s consumers, sorted by name.
    /// </summary>
    /// <remarks>
    /// ListConsumers returns one stream&#39;s consumers, sorted by name. 404 when the org has no stream of that name.
    /// </remarks>
    /// <param name="stream">Stream is the stream&#39;s name, from the path.</param>
    pplx::task<std::shared_ptr<Cloud_consumerPage>> cloudGetV1PubsubJetstreamStreamsStreamConsumers(
        utility::string_t stream
    ) const;
    /// <summary>
    /// GetConsumer returns one consumer of one org stream — its configuration and its cursor: delivered and acknowledged sequences, pending and redelivered counts.
    /// </summary>
    /// <remarks>
    /// GetConsumer returns one consumer of one org stream — its configuration and its cursor: delivered and acknowledged sequences, pending and redelivered counts. 404 when the stream or the consumer does not exist.
    /// </remarks>
    /// <param name="stream">Stream is the stream, from the path.</param>
    /// <param name="name">Name is the consumer, from the path.</param>
    pplx::task<std::shared_ptr<Cloud_consumerRecord>> cloudGetV1PubsubJetstreamStreamsStreamConsumersName(
        utility::string_t stream,
        utility::string_t name
    ) const;
    /// <summary>
    /// Get returns one key&#39;s current value and revision.
    /// </summary>
    /// <remarks>
    /// Get returns one key&#39;s current value and revision. 404 when the bucket does not exist, the key was never written, or its latest revision is a delete.
    /// </remarks>
    /// <param name="bucket">Bucket is the bucket, from the path.</param>
    /// <param name="key">Key is the key, from the path.</param>
    pplx::task<std::shared_ptr<Cloud_kvEntry>> cloudGetV1PubsubKvBucketKey(
        utility::string_t bucket,
        utility::string_t key
    ) const;
    /// <summary>
    /// History returns one key&#39;s retained revisions, oldest first — every put and every delete marker up to the bucket&#39;s History depth.
    /// </summary>
    /// <remarks>
    /// History returns one key&#39;s retained revisions, oldest first — every put and every delete marker up to the bucket&#39;s History depth. 404 when the bucket does not exist or the key was never written.
    /// </remarks>
    /// <param name="bucket">Bucket is the bucket, from the path.</param>
    /// <param name="key">Key is the key, from the path.</param>
    pplx::task<std::shared_ptr<Cloud_kvPage>> cloudGetV1PubsubKvBucketKeyHistory(
        utility::string_t bucket,
        utility::string_t key
    ) const;
    /// <summary>
    /// CreateStream creates a durable stream capturing the given subjects and returns it.
    /// </summary>
    /// <remarks>
    /// CreateStream creates a durable stream capturing the given subjects and returns it. 409 when the org already has a stream of that name; the subjects are the org&#39;s own and cannot collide with another org&#39;s.
    /// </remarks>
    /// <param name="cloudStreamWrite"></param>
    pplx::task<std::shared_ptr<Cloud_streamRecord>> cloudPostV1PubsubJetstreamStreams(
        std::shared_ptr<Cloud_streamWrite> cloudStreamWrite
    ) const;
    /// <summary>
    /// CreateConsumer creates a durable consumer on one stream and returns it.
    /// </summary>
    /// <remarks>
    /// CreateConsumer creates a durable consumer on one stream and returns it. A consumer is a named cursor: it tracks what has been delivered and what is acknowledged, so many workers can share it and none sees a message twice outside redelivery. 409 when the stream already has a consumer of that name with a different configuration.
    /// </remarks>
    /// <param name="stream">Stream is the stream to consume, from the path.</param>
    /// <param name="cloudConsumerWrite"></param>
    pplx::task<std::shared_ptr<Cloud_consumerRecord>> cloudPostV1PubsubJetstreamStreamsStreamConsumers(
        utility::string_t stream,
        std::shared_ptr<Cloud_consumerWrite> cloudConsumerWrite
    ) const;
    /// <summary>
    /// Fetch pulls the next batch from a consumer and acknowledges it — the request/response way to consume a stream.
    /// </summary>
    /// <remarks>
    /// Fetch pulls the next batch from a consumer and acknowledges it — the request/response way to consume a stream. The hand-off is at-most-once: a message returned here is acked here, so a caller that loses the response does not see it again. Workers needing at-least-once delivery consume the same consumer over the NATS port, where acks are theirs to send. An empty batch after the wait is an empty page, not an error.
    /// </remarks>
    /// <param name="stream">Stream is the stream, from the path.</param>
    /// <param name="name">Name is the consumer, from the path.</param>
    /// <param name="cloudFetchQuery"></param>
    pplx::task<std::shared_ptr<Cloud_messagePage>> cloudPostV1PubsubJetstreamStreamsStreamConsumersNameNext(
        utility::string_t stream,
        utility::string_t name,
        std::shared_ptr<Cloud_fetchQuery> cloudFetchQuery
    ) const;
    /// <summary>
    /// CreateBucket creates a KV bucket and returns it.
    /// </summary>
    /// <remarks>
    /// CreateBucket creates a KV bucket and returns it. A bucket is keyed state on the same durable plane as the streams: each key holds up to History revisions, entries can expire by TTL, and watchers on the NATS port see every write. 409 when the org already has a bucket of that name.
    /// </remarks>
    /// <param name="bucket">Bucket is the bucket&#39;s name within the org, from the path: 1–64 of [A-Za-z0-9_], no dash.</param>
    /// <param name="cloudBucketWrite"></param>
    pplx::task<std::shared_ptr<Cloud_bucketRecord>> cloudPostV1PubsubKvBucket(
        utility::string_t bucket,
        std::shared_ptr<Cloud_bucketWrite> cloudBucketWrite
    ) const;
    /// <summary>
    /// Publish puts one message on the org&#39;s bus.
    /// </summary>
    /// <remarks>
    /// Publish puts one message on the org&#39;s bus. When a stream captures the subject the write is DURABLE — the receipt names the stream and sequence only after JetStream has it on storage, and a repeated Nats-Msg-Id header within the dedup window answers duplicate instead of storing twice. When nothing captures it, the message goes out core NATS: delivered to current subscribers, receipt {ok}, nothing retained.
    /// </remarks>
    /// <param name="cloudBusPublish"></param>
    pplx::task<std::shared_ptr<Cloud_busAck>> cloudPostV1PubsubPublish(
        std::shared_ptr<Cloud_busPublish> cloudBusPublish
    ) const;
    /// <summary>
    /// Request sends one request on the org&#39;s bus and waits for one reply — the synchronous half of pub/sub, for callers speaking to a responder subscribed on the NATS port.
    /// </summary>
    /// <remarks>
    /// Request sends one request on the org&#39;s bus and waits for one reply — the synchronous half of pub/sub, for callers speaking to a responder subscribed on the NATS port. 404 when nobody is listening on the subject; 408 when a responder exists but no reply arrived within the timeout.
    /// </remarks>
    /// <param name="cloudBusRequest"></param>
    pplx::task<std::shared_ptr<Cloud_busMessage>> cloudPostV1PubsubRequest(
        std::shared_ptr<Cloud_busRequest> cloudBusRequest
    ) const;
    /// <summary>
    /// UpdateStream rewrites a stream&#39;s configuration — subjects, limits, discard — and returns the updated stream.
    /// </summary>
    /// <remarks>
    /// UpdateStream rewrites a stream&#39;s configuration — subjects, limits, discard — and returns the updated stream. It is a PUT: the spec sent replaces the spec held, with one reading for the enums a caller omits — an empty storage, retention or discard keeps the stream&#39;s current one, because JetStream holds storage and retention immutable and refuses a change with a 400 rather than this door pretending it took.
    /// </remarks>
    /// <param name="stream">Stream is the stream to update, from the path.</param>
    /// <param name="cloudStreamUpdate"></param>
    pplx::task<std::shared_ptr<Cloud_streamRecord>> cloudPutV1PubsubJetstreamStreamsStream(
        utility::string_t stream,
        std::shared_ptr<Cloud_streamUpdate> cloudStreamUpdate
    ) const;
    /// <summary>
    /// Put sets one key to one value and returns the revision the write created.
    /// </summary>
    /// <remarks>
    /// Put sets one key to one value and returns the revision the write created. Writes are versioned: each put is a new revision and the bucket retains up to its History of them per key.
    /// </remarks>
    /// <param name="bucket">Bucket is the bucket, from the path.</param>
    /// <param name="key">Key is the key, from the path.</param>
    /// <param name="cloudKvWrite"></param>
    pplx::task<std::shared_ptr<Cloud_kvAck>> cloudPutV1PubsubKvBucketKey(
        utility::string_t bucket,
        utility::string_t key,
        std::shared_ptr<Cloud_kvWrite> cloudKvWrite
    ) const;
    /// <summary>
    /// List active channels
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="pattern"> (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    pplx::task<std::shared_ptr<Kv_listChannels_200_response>> kvListChannels(
        boost::optional<utility::string_t> pattern
    ) const;
    /// <summary>
    /// Publish message to channel
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="kvPublishRequest"></param>
    pplx::task<std::shared_ptr<Kv_publish_200_response>> kvPublish(
        std::shared_ptr<Kv_publish_request> kvPublishRequest
    ) const;
    /// <summary>
    /// Subscribe to channels (SSE)
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="channels">Channels to subscribe to</param>
    /// <param name="pattern">Pattern to subscribe to (e.g. user:*) (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    pplx::task<utility::string_t> kvSubscribe(
        std::vector<utility::string_t> channels,
        boost::optional<utility::string_t> pattern
    ) const;

protected:
    std::shared_ptr<const ApiClient> m_ApiClient;
};

}
}

#endif /* HANZO_API_PubSubApi_H_ */

