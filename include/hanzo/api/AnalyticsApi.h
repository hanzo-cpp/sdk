/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * AnalyticsApi.h
 *
 * 
 */

#ifndef HANZO_API_AnalyticsApi_H_
#define HANZO_API_AnalyticsApi_H_



#include "hanzo/ApiClient.h"

#include "hanzo/model/Cloud_Overview.h"
#include "hanzo/model/Cloud_Timeseries.h"
#include "hanzo/model/Cloud_Top.h"
#include "hanzo/model/Cloud_healthReport.h"
#include "hanzo/model/Dns_Error.h"
#include "hanzo/model/Dns_QueryAnalytics.h"
#include <cpprest/details/basic_types.h>
#include <boost/optional.hpp>

namespace hanzo {
namespace api {

using namespace hanzo::model;



class  AnalyticsApi 
{
public:

    explicit AnalyticsApi( std::shared_ptr<const ApiClient> apiClient );

    virtual ~AnalyticsApi();

    /// <summary>
    /// Whether the analytics warehouse is reachable, and which lens tables exist
    /// </summary>
    /// <remarks>
    /// Reports the analytics subsystem&#39;s own liveness: whether the shared warehouse connection is up, and — when it is — whether each read lens&#39;s table has been provisioned yet (the LLM usage ledger and the product-event table, each named in the report).  IT DOES NOT PROBE THE WAREHOUSE TO EARN ITS 200. &#x60;datastore&#x60; here is the state of the process&#39;s own shared client — established, and not since closed — not the result of a query, so a warehouse that is accepting connections and failing reads still reports healthy. Degraded means only that the client never came up: 503, CARRYING the report (status, datastore:false, reason) as its body rather than an error envelope, so a readiness gate can read the cause off the same object it got at 200.  A MISSING LENS TABLE IS NOT A FAILURE and never moves the status. The per-lens probe runs only once connected, and a lens reported available:false answers honest-empty rather than erroring — a fresh deployment whose collector has not emitted yet is legitimately 200 with the product-event lens unavailable. The lens block is absent entirely from a degraded report, which has nothing to say about tables it could not reach.  Unauthenticated on purpose — liveness has to be probe-able — and it reads NO tenant data: table existence only, never a row.
    /// </remarks>
    pplx::task<std::shared_ptr<Cloud_healthReport>> cloudGetV1AnalyticsHealth(
    ) const;
    /// <summary>
    /// Overview returns the caller org&#39;s analytics KPIs for one time window.
    /// </summary>
    /// <remarks>
    /// Overview returns the caller org&#39;s analytics KPIs for one time window. Three lenses over one warehouse: llm is the live per-org LLM usage ledger (requests, tokens, spend, models, providers, errors) and is always real; web (pageviews, visitors, sessions) and commerce (orders, revenue, AOV) read the product-event table and report available&#x3D;false rather than fabricating zeros when it holds nothing yet.  The org is the validated principal&#39;s — never a parameter — so a caller can only ever read its own tenant. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
    /// </remarks>
    /// <param name="range">Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="start">Start is the inclusive lower bound of a custom window, RFC3339. Requires end. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="end">End is the exclusive upper bound of a custom window, RFC3339. Requires start. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    pplx::task<std::shared_ptr<Cloud_Overview>> cloudGetV1AnalyticsOverview(
        boost::optional<utility::string_t> range,
        boost::optional<utility::string_t> start,
        boost::optional<utility::string_t> end
    ) const;
    /// <summary>
    /// Timeseries returns the caller org&#39;s LLM usage over time as an evenly-spaced series.
    /// </summary>
    /// <remarks>
    /// Timeseries returns the caller org&#39;s LLM usage over time as an evenly-spaced series. One point per hour or per day — the bucket the window implies, 24h giving hours and 7d/30d giving days — carrying requests, total tokens and spend in cents. Empty buckets are filled with zeros so a client charts a continuous line.  The org is the validated principal&#39;s — never a parameter. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
    /// </remarks>
    /// <param name="range">Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="start">Start is the inclusive lower bound of a custom window, RFC3339. Requires end. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="end">End is the exclusive upper bound of a custom window, RFC3339. Requires start. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    pplx::task<std::shared_ptr<Cloud_Timeseries>> cloudGetV1AnalyticsTimeseries(
        boost::optional<utility::string_t> range,
        boost::optional<utility::string_t> start,
        boost::optional<utility::string_t> end
    ) const;
    /// <summary>
    /// Top returns the caller org&#39;s ranked lenses for one window, five of them at once.
    /// </summary>
    /// <remarks>
    /// Top returns the caller org&#39;s ranked lenses for one window, five of them at once. models ranks LLM models by spend and is always real; products ranks commerce orders by revenue; topPages ranks requested paths, topReferrers the external referrer domains (\&quot;(direct)\&quot; for a missing or same-origin one) and topSources the utm_source campaigns (\&quot;(none)\&quot; when absent), each by pageviews. Every lens carries each row&#39;s share of the in-window total, so a top-N honestly shows the long tail.  The four event lenses report available&#x3D;false rather than fabricating zeros when the product-event table holds nothing yet. The org is the validated principal&#39;s — never a parameter. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
    /// </remarks>
    /// <param name="range">Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="start">Start is the inclusive lower bound of a custom window, RFC3339. Requires end. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="end">End is the exclusive upper bound of a custom window, RFC3339. Requires start. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="limit">Limit bounds every ranked lens in the response. Default 10, maximum 100; a value at or below zero, or one that is not a number, takes the default. (optional, default to 0)</param>
    pplx::task<std::shared_ptr<Cloud_Top>> cloudGetV1AnalyticsTop(
        boost::optional<utility::string_t> range,
        boost::optional<utility::string_t> start,
        boost::optional<utility::string_t> end,
        boost::optional<int32_t> limit
    ) const;
    /// <summary>
    /// Get query analytics
    /// </summary>
    /// <remarks>
    /// 
    /// </remarks>
    /// <param name="zone"></param>
    /// <param name="from"> (optional, default to utility::datetime())</param>
    /// <param name="to"> (optional, default to utility::datetime())</param>
    /// <param name="granularity"> (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    pplx::task<std::shared_ptr<Dns_QueryAnalytics>> dnsGetZoneAnalytics(
        utility::string_t zone,
        boost::optional<utility::datetime> from,
        boost::optional<utility::datetime> to,
        boost::optional<utility::string_t> granularity
    ) const;

protected:
    std::shared_ptr<const ApiClient> m_ApiClient;
};

}
}

#endif /* HANZO_API_AnalyticsApi_H_ */

