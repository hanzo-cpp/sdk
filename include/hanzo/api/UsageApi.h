/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/*
 * UsageApi.h
 *
 * 
 */

#ifndef HANZO_API_UsageApi_H_
#define HANZO_API_UsageApi_H_



#include "hanzo/ApiClient.h"

#include "hanzo/model/Cloud_ActivityView.h"
#include "hanzo/model/Cloud_LeaderboardView.h"
#include "hanzo/model/Cloud_backfillQuery.h"
#include "hanzo/model/Cloud_backfillResult.h"
#include "hanzo/model/Cloud_dashResp.h"
#include "hanzo/model/Cloud_optinView.h"
#include "hanzo/model/Cloud_orgOptinReq.h"
#include "hanzo/model/Cloud_orgOptinView.h"
#include "hanzo/model/Cloud_reportReq.h"
#include "hanzo/model/Cloud_reportResp.h"
#include "hanzo/model/Cloud_usageAnalyticsAccess.h"
#include "hanzo/model/Cloud_usageAnalyticsView.h"
#include "hanzo/model/Cloud_usageSummary.h"
#include "hanzo/model/Cloud_userOptinReq.h"
#include "hanzo/model/Cloud_userOptinView.h"
#include <cpprest/details/basic_types.h>
#include <boost/optional.hpp>

namespace hanzo {
namespace api {

using namespace hanzo::model;



class  UsageApi 
{
public:

    explicit UsageApi( std::shared_ptr<const ApiClient> apiClient );

    virtual ~UsageApi();

    /// <summary>
    /// Activity returns the per-day usage series for ONE authorized subject — the points a contribution heatmap and a timeline are drawn from, gap-filled so every day in the range is present.
    /// </summary>
    /// <remarks>
    /// Activity returns the per-day usage series for ONE authorized subject — the points a contribution heatmap and a timeline are drawn from, gap-filled so every day in the range is present. Authorization is resolved server-side from the validated principal, so a caller can never widen the subject past what they are entitled to: a non-admin reads only themselves and their own org. subject&#x3D;project answers empty with a note, because the usage ledger records no project column yet. When the warehouse is not connected the series answers empty with available&#x3D;false rather than fabricated days.
    /// </remarks>
    /// <param name="subject">Subject is what the series is about: \&quot;user\&quot; (default), \&quot;org\&quot; or \&quot;project\&quot;. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="id">ID names the subject within what the caller is entitled to see. Omitted (or \&quot;me\&quot;) it is the caller themselves, or their own org. Another user requires org admin and must belong to the caller&#39;s org; another org requires a SuperAdmin. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="from">From is the first day of the range, \&quot;2006-01-02\&quot;. Defaults to 90 days back. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="to">To is the last day of the range, \&quot;2006-01-02\&quot;. Defaults to today; the span is clamped to 366 days. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    pplx::task<std::shared_ptr<Cloud_ActivityView>> cloudGetV1UsageActivity(
        boost::optional<utility::string_t> subject,
        boost::optional<utility::string_t> id,
        boost::optional<utility::string_t> from,
        boost::optional<utility::string_t> to
    ) const;
    /// <summary>
    /// Is the entitlement-GATED per-provider breakdown of the caller org&#39;s LLM usage — the paid lens over the same warehouse ledger GET /v1/usage/summary reads its totals from.
    /// </summary>
    /// <remarks>
    /// Is the entitlement-GATED per-provider breakdown of the caller org&#39;s LLM usage — the paid lens over the same warehouse ledger GET /v1/usage/summary reads its totals from. Basic own-org usage stays ungated at /v1/usage/summary.  A plan that does not grant the analytics datastore is refused with 402, and an unresolvable plan fails closed to the free floor, which does not grant it. The window is clamped forward to the plan&#39;s retention entitlement, so a tenant can never read older than its plan allows even with a custom start. The response is marked no-store.  INTERIM (mirrors apps/world&#39;s limits echo): no org→plan resolver exists in cloud yet — the subscription lookup is owned by the billing plane and the gateway principal carries no plan claim — so the caller passes the plan and the gate resolves THAT plan&#39;s access.
    /// </remarks>
    /// <param name="end">End is the exclusive window end, RFC3339. Read only when Range is custom. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="plan">Plan is the plan id whose entitlement decides access and retention. INTERIM: cloud has no org-to-plan resolver yet, so the caller names the plan; when that resolver lands this becomes the caller org&#39;s own plan. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="range">Range is the window: 24h, 7d, 30d, or custom. Empty means 24h. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="start">Start is the inclusive window start, RFC3339. Read only when Range is custom, and clamped forward to the plan&#39;s retention floor. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    pplx::task<std::shared_ptr<Cloud_usageAnalyticsView>> cloudGetV1UsageAnalytics(
        boost::optional<utility::string_t> end,
        boost::optional<utility::string_t> plan,
        boost::optional<utility::string_t> range,
        boost::optional<utility::string_t> start
    ) const;
    /// <summary>
    /// Echoes a plan&#39;s resolved analytics entitlement so a dashboard can configure itself against the LIVE catalog instead of hardcoding tier numbers.
    /// </summary>
    /// <remarks>
    /// Echoes a plan&#39;s resolved analytics entitlement so a dashboard can configure itself against the LIVE catalog instead of hardcoding tier numbers. An empty plan resolves the free floor, and a catalog resolution failure serves that same floor rather than erroring — so this always answers 200. It is a read-only contract echo and carries no tenant data.
    /// </remarks>
    /// <param name="plan">Plan is a plan id from the live @hanzo/plans catalog. Empty resolves the free floor, and so does an id the catalog does not know — this never fails on an unknown plan. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    pplx::task<std::shared_ptr<Cloud_usageAnalyticsAccess>> cloudGetV1UsageAnalyticsAccess(
        boost::optional<utility::string_t> plan
    ) const;
    /// <summary>
    /// Leaderboard ranks AI usage over a window, either the users of the caller&#39;s own org or organizations against each other, and always reports the caller&#39;s own standing even when it falls outside the returned page.
    /// </summary>
    /// <remarks>
    /// Leaderboard ranks AI usage over a window, either the users of the caller&#39;s own org or organizations against each other, and always reports the caller&#39;s own standing even when it falls outside the returned page. Identities are private by default: a caller sees themselves, plus the peers or orgs that opted into public listing, and only an admin sees their own org&#39;s members named. Cross-org spend is restricted to platform admins. When the warehouse is not connected the board answers empty with available&#x3D;false rather than a fabricated rank.
    /// </remarks>
    /// <param name="scope">Scope picks the board: \&quot;personal\&quot; (default) ranks the caller among their own org&#39;s users, \&quot;org\&quot; is that same org board named for an admin, \&quot;global\&quot; ranks organizations against each other. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="metric">Metric is the value ranked: tokens (default), requests, or cost. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="period">Period is the window ranked: day, week, month (default) or all. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="limit">Limit caps the rows returned, clamped to 100. Defaults to 10, which is also what a non-positive or unparseable value takes. (optional, default to 0)</param>
    pplx::task<std::shared_ptr<Cloud_LeaderboardView>> cloudGetV1UsageLeaderboard(
        boost::optional<utility::string_t> scope,
        boost::optional<utility::string_t> metric,
        boost::optional<utility::string_t> period,
        boost::optional<int32_t> limit
    ) const;
    /// <summary>
    /// GetOptin returns the caller&#39;s own public-listing preference and their org&#39;s, each with whether the caller may change it.
    /// </summary>
    /// <remarks>
    /// GetOptin returns the caller&#39;s own public-listing preference and their org&#39;s, each with whether the caller may change it. Public listing is opt-in and private by default, so a fresh caller reads listed&#x3D;false for both.
    /// </remarks>
    pplx::task<std::shared_ptr<Cloud_optinView>> cloudGetV1UsageLeaderboardOptin(
    ) const;
    /// <summary>
    /// Is the PER-PROVIDER view: one connected account&#39;s own consumption of its own plan — \&quot;my Claude Max plan is 47% through its 6h window, resets at 14:20\&quot;.
    /// </summary>
    /// <remarks>
    /// Is the PER-PROVIDER view: one connected account&#39;s own consumption of its own plan — \&quot;my Claude Max plan is 47% through its 6h window, resets at 14:20\&quot;.  &#x60;current&#x60; is the newest instance of each lane (the headline); &#x60;windows&#x60; is the history behind it. Both come from ONE deduped read, so they can never disagree. The rows are the caller&#39;s OWN linked accounts, scoped to the validated principal and its subject — never another user&#39;s, and never another org&#39;s.
    /// </remarks>
    /// <param name="account">Account narrows to ONE linked account of that provider. Empty covers every account the caller has linked there. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="provider">Provider is the upstream to read, e.g. anthropic. Required. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="range">Range is the window to read: 1h, 24h, 7d or 30d. Empty means 24h, and any other label is refused rather than silently replaced. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="window">Window narrows to ONE window class: 6h, day, week or month. Empty covers every class. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    pplx::task<std::shared_ptr<Cloud_dashResp>> cloudGetV1UsageSamples(
        boost::optional<utility::string_t> account,
        boost::optional<utility::string_t> provider,
        boost::optional<utility::string_t> range,
        boost::optional<utility::string_t> window
    ) const;
    /// <summary>
    /// Answers GET /v1/usage/summary: the caller&#39;s own usage footprint over one window — the categorized spend roll-up from the commerce ledger, the org&#39;s LLM usage totals from the warehouse, and the caller&#39;s OWN linked provider accounts beside the org&#39;s Hanzo-routed usage.
    /// </summary>
    /// <remarks>
    /// Answers GET /v1/usage/summary: the caller&#39;s own usage footprint over one window — the categorized spend roll-up from the commerce ledger, the org&#39;s LLM usage totals from the warehouse, and the caller&#39;s OWN linked provider accounts beside the org&#39;s Hanzo-routed usage.  Every source degrades INDEPENDENTLY to honest zeros and says so in &#x60;sources&#x60; and in its own &#x60;available&#x60; flag, so a partial deploy reports \&quot;no data\&quot; rather than fabricating spend. The account rows and the Hanzo rows are concatenated and never summed: a plan&#39;s percent is not money.  The response is org-scoped from the validated principal and marked no-store — a signed-out caller is refused.
    /// </remarks>
    /// <param name="range">Range is the window: 24h, 7d, 30d, or custom. Empty means 24h. A label this surface does not know is refused rather than silently replaced. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="start">Start is the inclusive window start, RFC3339. Read only when Range is custom. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    /// <param name="end">End is the exclusive window end, RFC3339. Read only when Range is custom. (optional, default to utility::conversions::to_string_t(&quot;&quot;))</param>
    pplx::task<std::shared_ptr<Cloud_usageSummary>> cloudGetV1UsageSummary(
        boost::optional<utility::string_t> range,
        boost::optional<utility::string_t> start,
        boost::optional<utility::string_t> end
    ) const;
    /// <summary>
    /// Ingests a batch of account-usage samples — what a developer&#39;s OWN AI accounts have consumed of their OWN plans, metered from each provider&#39;s own login — and appends them to the warehouse series.
    /// </summary>
    /// <remarks>
    /// Ingests a batch of account-usage samples — what a developer&#39;s OWN AI accounts have consumed of their OWN plans, metered from each provider&#39;s own login — and appends them to the warehouse series. Answers 202.  Send either a &#x60;samples&#x60; array or one sample&#39;s fields at the top level. Every sample needs a provider, a machine and a known window class; an unknown window or kind is refused rather than silently rewritten, because a dash filled with a class nobody reported is worse than an error. There is no timestamp field: the server owns the observation clock, and a sample says which window it measured with windowStart or resetsAt.  It is FAIL-SOFT on storage: a warehouse outage costs a poll of history (stored:false), never a failed request. It records usage ONLY — the link registry is refreshed separately via POST /v1/links, so there is one and only one way to update an account row.
    /// </remarks>
    /// <param name="cloudReportReq"></param>
    pplx::task<std::shared_ptr<Cloud_reportResp>> cloudPostV1Usage(
        std::shared_ptr<Cloud_reportReq> cloudReportReq
    ) const;
    /// <summary>
    /// Backfill seeds the derived usage rollup from ledger history — the rows written before the incremental view existed, which that view can never capture.
    /// </summary>
    /// <remarks>
    /// Backfill seeds the derived usage rollup from ledger history — the rows written before the incremental view existed, which that view can never capture. SuperAdmin only. Because the rollup accumulates, a second unguarded run would double every day it re-reads, so it refuses with 409 when the rollup already holds rows unless force&#x3D;true is passed; forcing WILL double-count.
    /// </remarks>
    /// <param name="cloudBackfillQuery"></param>
    pplx::task<std::shared_ptr<Cloud_backfillResult>> cloudPostV1UsageRollupBackfill(
        std::shared_ptr<Cloud_backfillQuery> cloudBackfillQuery
    ) const;
    /// <summary>
    /// PutUserOptin sets the CALLER&#39;s own public-listing preference on the leaderboard.
    /// </summary>
    /// <remarks>
    /// PutUserOptin sets the CALLER&#39;s own public-listing preference on the leaderboard. Self only: the row written is keyed by the caller&#39;s validated ledger identity, so this can never edit another member&#39;s visibility whatever the request says. A caller opting in with no handle is given their username, so a listed row never renders as \&quot;Anonymous\&quot; to its own owner.
    /// </remarks>
    /// <param name="cloudUserOptinReq"></param>
    pplx::task<std::shared_ptr<Cloud_userOptinView>> cloudPutV1UsageLeaderboardOptin(
        std::shared_ptr<Cloud_userOptinReq> cloudUserOptinReq
    ) const;
    /// <summary>
    /// PutOrgOptin sets the ORG&#39;s listing on the cross-org global board.
    /// </summary>
    /// <remarks>
    /// PutOrgOptin sets the ORG&#39;s listing on the cross-org global board. Only an admin of the caller&#39;s own org — an org admin or a platform SuperAdmin — may change it, and the org written is the caller&#39;s validated tenant, never a value from the request. Listing consents to publishing the org&#39;s usage VOLUME; cross-org spend stays restricted to platform admins regardless.
    /// </remarks>
    /// <param name="cloudOrgOptinReq"></param>
    pplx::task<std::shared_ptr<Cloud_orgOptinView>> cloudPutV1UsageLeaderboardOptinOrg(
        std::shared_ptr<Cloud_orgOptinReq> cloudOrgOptinReq
    ) const;

protected:
    std::shared_ptr<const ApiClient> m_ApiClient;
};

}
}

#endif /* HANZO_API_UsageApi_H_ */

